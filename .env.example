NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required only for the AI Assistant page (/assistant). Free, no credit
# card required — get a key from https://aistudio.google.com/apikey.
# Never exposed to the browser — only read server-side in lib/ai/gemini.ts.
GEMINI_API_KEY=

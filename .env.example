NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required only for the AI Assistant page (/assistant). Get a key from
# https://console.anthropic.com/settings/keys. Never expose this to the
# browser — it's only ever read server-side in app/(app)/assistant/actions.ts.
ANTHROPIC_API_KEY=

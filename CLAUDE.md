@AGENTS.md

# Command Deck v2 — Build Spec

## 0. What this is
A personal finance web app, rebuilt from scratch as a deliberately small, Apple-designed tool. The original app (documented in the Build Log below, kept for history) grew to ~27 routes across many incremental sessions — MFA, month-end close, tax reports, forecasting, audit log, multi-currency, backups, CSV import, and more. Abel asked to "completely change the whole script": a full ground-up rebuild, not another reskin, driven by two things — the app had gotten too complex, and none of the seven prior visual redesigns (all documented below) actually felt right. This version starts over with a hard constraint on scope (5 pages, one unified ledger table) and a real, HIG-accurate Apple design language rather than an approximation of one.

This was a genuine fresh start: the previous Supabase schema (17 tables), all 3 pg_cron jobs, and all 3 real signed-up `auth.users` rows (including Abel's own account and its data) were dropped/deleted as part of this rebuild, with Abel's explicit confirmation that this loss was acceptable.

## 1. Stack
Unchanged from v1 — nothing about the stack was the problem, only complexity and design.
- **Framework:** Next.js 16 (App Router), TypeScript — `proxy.ts` (not `middleware.ts`), exported function name `proxy`. `cookies()`/`headers()`/`params`/`searchParams` are all async.
- **Styling:** Tailwind CSS v4 (CSS-first config, no `tailwind.config.js`)
- **DB/Auth:** Supabase (Postgres + Supabase Auth, email/password, open signup — multi-tenant)
- **Hosting:** Vercel, auto-deploys previews on push via GitHub integration
- **No charts.** v1 used Recharts across several pages; v2 deliberately has none — the 5-page scope doesn't need them, and Apple's own Settings/Wallet-adjacent apps lean on numbers, lists, and simple progress bars rather than charts for this kind of everyday-finance surface. The dependency was removed from `package.json`.

## 2. Design system — real Apple HIG, not an approximation

The core instruction was "how Apple would design something like this, one to one copy" — meaning actual HIG values, not a vibe. Supports **both light and dark**, following the system automatically (`prefers-color-scheme`), which is itself more authentically Apple than the several static-dark-only design systems v1 went through.

### Tokens (`app/globals.css`)
- Full systemGray scale (`--gray` … `--gray6`), separate light/dark values.
- Tinted label hierarchy exactly matching Apple's own opacity-based system: `--label` (solid), `--label-secondary` (60% over the label color), `--label-tertiary` (30%), `--label-quaternary` (18%) — not distinct hex approximations.
- Real separator (`rgba` over gray, not a flat hex), system fill tiers, systemBlue/Green/Red/Orange/Purple/Indigo/Teal/Pink at their real light and dark values.
- System font stack (`-apple-system, "SF Pro Text", "SF Pro Display", …`) — no downloaded typeface. Native apps don't ship a custom font either, and this also means every native weight is available for free (a real bug category from an earlier v1 redesign — "does this weight exist in the loaded set" — is structurally impossible now).
- **Money renders in the body font with `tabular-nums`, not a mono font.** SF Mono is for code; real iOS shows currency in tabular SF Pro.
- Full SF text-style ramp as classes: `.ios-large-title` (34/700), `.ios-title2`, `.ios-title3`, `.ios-headline` (17/600), `.ios-body`, `.ios-subhead`, `.ios-footnote`, `.ios-caption`, `.section-header`.

### Material vs. solid
`.glass` is a **solid** grouped-list card (`--bg-elevated`, 14px radius, hairline border) — the real Settings/Health app look. `.material` (blur + saturate) is reserved for **overlay chrome only** — the mobile tab bar, the Quick Add sheet, the date-picker popover — never for content cards. This is the correct HIG distinction; v1 got this wrong on its first Apple-styled pass and had to correct it in a later "deep-detail" pass.

### Components
Real iOS switch (51×31, systemGreen when on), real segmented control (sliding thumb via `data-active`), a bubbly Apple-style date picker (`components/ui/DatePicker.tsx`, portaled calendar popover, carried over unchanged from v1 since it was already correct and has no schema dependency), 10px corner radii on inputs/buttons (rounded rect, not a pill and not architectural), large-title in-content headers (no redundant top app-bar).

### The one rule that has survived every redesign this project has ever done
No color-coding for good/bad beyond real, deliberate use of systemGreen/systemRed for income/expense amounts and destructive actions — direction (↑/↓, +/−) and weight still do most of the signaling work. This is now the sixth-or-so design system this project has shipped; this restraint is the one thing that has survived every single one.

## 3. Data model (Supabase tables)

The biggest structural simplification versus v1: **one unified `entries` table** replaces the old separate `transactions` / `income` / `transfers` / `recurring_bills` / `recurring_income` tables. A `type` column (`'expense' | 'income' | 'transfer'`) distinguishes the three cash-flow directions, and an `is_recurring` flag marks predictable ones — there is no separate cron-driven auto-posting system; recurring items are just entries with a flag, surfaced on the Budgets page with a manual "Log this month" action.

```sql
settings        (id, user_id, currency_code default 'USD', low_balance_threshold, created_at)
accounts        (id, user_id, name, kind 'asset'|'liability', starting_balance, sort_order)
categories      (id, user_id, name, monthly_budget, sort_order)
entries         (id, user_id, date, type, amount, account_id, to_account_id,
                  category_id, description, notes, is_recurring, created_at)
savings_goals   (id, user_id, name, target_amount, target_date, account_id, saved_so_far)
```

Every table has `user_id uuid not null references auth.users(id)` and an RLS policy `using ((select auth.uid()) = user_id)` — the `select`-wrapped form per Supabase's RLS perf guidance, carried over from v1's own hard-won lesson on this.

Two computed views, both `security_invoker = true` so they respect the querying user's RLS automatically:
- `account_balance` — live net worth per account (`starting_balance + income − expenses ± transfers`, liabilities sign-flipped), no manual snapshot/reconciliation step.
- `budget_vs_actual_this_month` — per-category budget vs. actual spend this month.

`handle_new_user()` (a trigger on `auth.users`, `security definer`, RPC access revoked from `public`/`anon`/`authenticated`) auto-provisions every new signup: one settings row, two starter accounts (Checking/Savings), and 8 generic default categories. Signup is fully open — no invite gate, matching v1's later multi-tenant conversion.

Verified via a rollback-only SQL test (the standing discipline on this project): signup provisioning, an expense + income + transfer, a linked savings goal, and both views' math all check out — `begin; ...; rollback;` in one atomic call, zero residue confirmed afterward.

## 4. Pages (6, down from ~27 — originally launched at 5; Assistant added post-launch)

1. **Home (`/`)** — hero net-worth card (live, not a snapshot), this month's spent/income, an account list, recent activity. Quick Add is a floating action button available everywhere. Shows a live-converted secondary-currency line (e.g. `≈ ETB ...`) under each figure when Settings has one configured.
2. **Transactions (`/transactions`)** — the one unified ledger. Filter by type (All/Expense/Income/Transfer), search, inline edit, delete, CSV export.
3. **Budgets (`/budgets`)** — category budgets vs. actual this month (inline-editable budget amounts, a progress bar per category) plus a **Recurring** section listing entries flagged `is_recurring`, each with a one-tap "Log this month" action.
4. **Goals (`/goals`)** — savings goals with progress bars; a goal can either track a real linked account live (`account_balance`) or a manually-entered "saved so far" amount.
5. **Assistant (`/assistant`)** — a read-only AI chat grounded in the user's real data (balances, budgets, goals, ~90 days of entries). Can't add/edit/delete anything. Needs `GEMINI_API_KEY` set to actually respond.
6. **Settings (`/settings`)** — accounts (name, asset/liability, starting balance — this is where net worth's components live), categories, main + optional secondary currency code, email + sign out, one-click full data export (JSON).

Auth (`/login`, `/signup`) and `/privacy`/`/terms` are unchanged in structure from v1, just trimmed of references to cut features (MFA, receipt uploads, self-serve account deletion).

## 5. What was cut, and why

Everything below existed in v1 and was deliberately not rebuilt, because it doesn't fit "essentials + a couple extras" at v2's scope: MFA/TOTP, backup & restore, month-end close & period locking, tax-time reporting, forecasting & scenarios, audit log, multi-currency (beyond a single `currency_code` display preference), transfers/weekly-rollup/monthly-rollup/net-worth as their own dedicated pages (net worth folded into Home; transfers folded into the unified ledger), CSV bank import, global search (⌘K), the onboarding modal, email digest/notifications, receipt attachments, PWA install-prompt polish beyond what already existed. The two "couple extras" kept beyond a bare MVP ledger were **net worth** (live, on Home) and **recurring bills/income** (the Recurring section of Budgets) — a judgment call flagged to Abel at plan time since he didn't specify which extras, not overridden since.

`/accounts/[id]` (the old Wallet-card-stack per-account detail page) was also cut — account management lives in Settings now, and Home's account list is a plain list, not a card-stack metaphor (that was specific to the "Apple Wallet" v1 redesign, not this HIG-general one).

## 6. Non-goals (unchanged from v1)
No multi-user sharing/permissions, no bank account sync (Plaid, etc.) — manual entry only, no native mobile app (responsive PWA-feel web app is enough).

## 7. Build log / session decisions (v2)

- **Rebuild executed via Supabase MCP directly against the live project** (`xgmznitqwcvjgeoyghor`, "Bigboy money count") — `drop schema public cascade` + `cron.unschedule` on all 3 jobs + `delete from auth.users` for all 3 real accounts, then the fresh 5-table schema applied as a single migration (`v2_schema`). A grants migration (`v2_grants`) was needed immediately after — recreating the `public` schema from scratch drops Supabase's default `anon`/`authenticated`/`service_role` table grants along with it, which isn't obvious until the first RLS-scoped query fails with a bare `permission denied for table` (not an RLS-policy error) rather than an empty result.
- **`get_advisors` (security) came back completely clean** after the schema + grants migrations — zero findings, including the two long-standing pre-existing v1 items (`rls_auto_enable`, leaked-password-protection) which no longer apply to a freshly-created project's advisor baseline the same way.
- **The empty `receipts` storage bucket from v1 was left in place**, not deleted — Supabase's `storage.protect_delete()` guard blocks direct SQL `DELETE`/bucket-drop even when the bucket is empty (confirmed by checking `count(*) from storage.objects where bucket_id = 'receipts'` = 0 first), and receipts aren't a v2 feature, so it's inert and harmless rather than worth fighting the Storage API for.
- **Frontend rebuild strategy**: reused everything with zero v1-schema dependency verbatim — `lib/supabase/{client,server}.ts`, `components/ui/{DatePicker,HScroll,PullToRefresh}.tsx`, `components/{AuthTabs,LoginForm,SignupForm,InstallPrompt,ServiceWorkerRegister}.tsx`, the login/signup page shells, the PWA manifest/icon routes (re-tinted to systemBlue/black). Everything else — `globals.css`, nav, Quick Add, all 5 page routes and their table/form components — was deleted and rebuilt fresh against the new schema and design language.
- **`lib/supabase/proxy.ts` lost its MFA step-up gate** (the `getAuthenticatorAssuranceLevel()` redirect block) since MFA isn't a v2 feature; `/mfa-challenge` and its actions were deleted outright. `proxy.ts`'s route matcher comment was trimmed of its now-inaccurate cron-route explanation (the `/api/cron/**` routes and their carve-out are gone — there's no more `app/api` directory at all).
- **`package.json` trimmed**: `recharts` and `xlsx` dependencies removed (no charts, no xlsx importer in v2), `migrate:xlsx` script removed, package renamed `command-deck` at version `2.0.0`. `vercel.json` deleted outright — it only ever held the two now-cut notification cron schedules.
- **Verified**: `tsc --noEmit` / `npm run lint` / `npm run build` all clean (13 routes, down from 27); a full grep sweep for dangling references to every cut feature (old route paths, `fmtUsd`, `PAYMENT_METHODS`/`INCOME_SOURCES`, `chart-colors`, `AlertsBanner`, `GlobalSearch`, `OnboardingModal`, `MfaChallengeForm`, `ImportForm`, wallet-card components, old table components, etc.) came back empty; `/login` screenshotted in both light and dark via the pre-installed headless Chromium against the real compiled CSS, confirming the grouped-card look, systemBlue button, and segmented control all render correctly in both themes. A live end-to-end signup-and-click-through via Playwright was attempted against the real Supabase project but blocked by this sandbox's egress policy (outbound HTTPS to the Supabase host returns a 403 from the environment's proxy) — this is a known, standing limitation of this sandbox (documented repeatedly through v1's build log) and not something to route around, so verification fell back to the DB-layer rollback-only test plus the static-CSS screenshot method already established as this project's standard.
- **Post-launch: ETB (secondary currency) display + a read-only AI Assistant.** Abel asked what else to add and named both himself. Scoped via `AskUserQuestion`: the exchange rate should be **live** (not manually entered), and the assistant should **answer questions about spending, read-only** (not add entries or push unsolicited suggestions, both offered as options but not picked).
  - **Secondary currency**: `settings.secondary_currency_code` (nullable text, `add_secondary_currency` migration) — when set, Home shows a `≈ <converted amount>` line under Net Worth, Spent This Month, and Income This Month. Rate comes from `lib/fx.ts`, calling `open.er-api.com`'s free, keyless endpoint (no API key, no cost, updated roughly every 24h) rather than the earlier `AskUserQuestion` round's "live API" option implying a paid/keyed service — this one happened to be free, which is strictly better than what was scoped. Cached via Next's `fetch` data cache (`revalidate: 3600`) so it's not refetched every request. Settings → Currency gained a second input for it (blank = off); Abel's own settings row was set to `ETB` directly via SQL as a courtesy since he'd explicitly asked for exactly that.
  - **AI Assistant** (`/assistant`, new 6th nav destination — `NAV_LINKS` and `components/nav/icons.tsx` both just needed the one new entry since `AppShell` already maps over the list generically): a chat UI (`components/AssistantChat.tsx`) backed by a server action (`app/(app)/assistant/actions.ts`) that calls Google's Gemini API directly via `fetch` (`lib/ai/gemini.ts`, `gemini-2.5-flash`, no SDK dependency added) using a `GEMINI_API_KEY` env var. Every question is grounded in a fresh, bounded read-only snapshot of the user's real data (`lib/ai/spending-context.ts` — live account balances, this month's budget-vs-actual, savings goals, and the last ~90 days / 150 entries), rebuilt on every message rather than cached, so answers can't go stale mid-conversation. System prompt explicitly forbids inventing numbers, states the assistant can't add/edit/delete anything (redirects to the + button instead), and disclaims that it isn't professional financial advice. **Honestly scaffolded, not silently broken**: `askGemini()` throws a clear "isn't set up yet" message when `GEMINI_API_KEY` is absent, surfaced in the chat UI as a normal error bubble rather than a crash — same honest-scaffolding pattern this project used for the earlier email-notification cron routes.
  - **Provider swap, same turn**: first built against the Anthropic API directly, then Abel asked specifically for a free-tier provider. Swapped to Google's Gemini API (`aistudio.google.com` — genuinely free, no credit card required, generous rate limits) rather than Anthropic's paid-only API or a rate-limited free-model aggregator (e.g. Groq's free tier was considered but Gemini's quota and model quality were the better fit for a low-volume personal app). The provider-specific bits are isolated to `lib/ai/gemini.ts` (Gemini's `contents`/`system_instruction` request shape and `user`/`model` role naming, vs. Anthropic's `messages`/`system` and `user`/`assistant`); `ChatMessage` moved to its own `lib/ai/types.ts` so the chat UI and server action don't import from a provider-named file.
  - **Caught by `get_advisors`, not assumed**: adding the `secondary_currency_code` column resurfaced a real bug from the original v2 rebuild — the `v2_grants` migration's blanket `grant all on all functions in schema public to anon, authenticated, service_role` had silently re-granted direct RPC execute on `handle_new_user()` to `anon`/`authenticated`, undoing the explicit revoke at the end of `v2_schema.sql` (Postgres still fires the trigger internally regardless of the grant, so this was an RPC-exposure risk, not a broken trigger). Fixed via `fix_handle_new_user_grants`, `get_advisors` re-run clean afterward.
  - **`supabase/migrations/` gap closed**: the original v2 rebuild applied `v2_full_reset_drop_everything`/`v2_schema`/`v2_grants` via Supabase MCP but never wrote the matching `.sql` files to the repo, breaking this project's own standing "raw SQL lives in migrations for the record" discipline. All three (plus the two from this session) were written to `supabase/migrations/` with their real applied timestamps, confirmed against Supabase's own migration ledger (`list_migrations`) rather than guessed — replaying the full 45-file history in order from empty is still valid, since `v2_full_reset_drop_everything` really does `drop schema public cascade` partway through, so the old v1 files aren't misleading dead weight, just an accurate record of "built v1, wiped it, built v2."
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` clean (14 routes, up from 13); `get_advisors` clean except the one pre-existing dashboard-only item; a rollback-only SQL test confirmed the new column reads/writes correctly under RLS; grep sweep clean. The AI Assistant's live behavior (and the ETB conversion's real numbers) couldn't be click-tested from this sandbox for the same standing egress-policy reason as every prior phase — verified by code review instead (error-path handling when the API key is absent, context-freshness, RLS scoping of the data snapshot).
  - **Groq fallback + keyed FX upgrade, same session.** Abel supplied a Groq key and an exchangerate-api.com key directly in chat. Neither was ever written to a file — this repo is public, so a committed secret would be permanently exposed in git history — both stay Vercel-dashboard-only env vars, added by Abel himself since no available tool here can set them. `lib/ai/groq.ts` (OpenAI-compatible `chat/completions` shape, `llama-3.3-70b-versatile`) is now an automatic fallback in `askAssistant()`: Gemini is tried first, and only on failure (unset key or a request error) does it fall through to Groq, so having both configured buys real redundancy rather than an either/or choice. `lib/fx.ts` similarly now prefers `EXCHANGERATE_API_KEY`'s keyed v6 endpoint (`v6.exchangerate-api.com`, 1,500 free requests/month) when set, falling back to the original keyless `open.er-api.com` otherwise — so the currency feature keeps working with zero setup either way, this just makes it more headroom-rich once a key is added.

---

# Everything below this line describes Command Deck v1 (superseded)

The app was fully rebuilt from scratch on 2026-07-20 (see the v2 spec and Build Log above). Everything below is the historical record of what v1 was and why — the schema, routes, and design systems it describes no longer exist in the live app or database, but the reasoning is kept because it explains real lessons (RLS/security-definer patterns, the rollback-only testing discipline, several real bugs found and fixed) that generalize beyond the specific v1 implementation.

@AGENTS.md

# VMI Finance Command Center — Build Spec

## 0. What this is
A personal finance web app for Abel, replacing the `Expense_Tracker.xlsx` workbook (12-tab dual-currency cadet budget tracker). Full parity with the spreadsheet's logic, rebuilt as a real app: one source-of-truth `transactions`/`income` table, everything else (rollups, insights, net worth) is a computed view — not a manually-synced duplicate sheet.

Reference file for exact field names/logic: `Expense_Tracker.xlsx` (the 12-tab version — see Build Log below, there was an older 5-tab `VMI_Expense_Tracker.xlsx` draft that is superseded and not used).

## 1. Stack
- **Framework:** Next.js 16 (App Router), TypeScript — note: `middleware.ts` is deprecated in v16, renamed to `proxy.ts` (exported function name `proxy`, not `middleware`). See `AGENTS.md`.
- **Styling:** Tailwind CSS v4 (CSS-first config, no `tailwind.config.js`)
- **DB/Auth:** Supabase (Postgres + Supabase Auth, email/password, single user — no public signup)
- **Hosting:** Vercel, subdomain `finance.abelatnafu.com`
- **Charts:** Recharts (weekly/monthly trend lines, category breakdown donut, semester pacing bar)

## 2. Design system — Liquid Glass Monochrome

**Superseded — see Build Log "Neo-Luxury redesign" below for the current, live design system.** This section is kept as historical record of the original design pass; the app no longer implements it.

Locked in from design pass. Reference build: `command-deck-mono.html` — build the component library to match it exactly, don't reinterpret. (Uploaded to the repo build session; the exact CSS from it is transcribed verbatim into `app/globals.css`.)

**This is NOT generic glassmorphism.** Generic glassmorphism = one flat `backdrop-filter: blur()` on a translucent box. Liquid glass = layered depth (multiple blur/opacity tiers so panels sit at different "densities"), an ambient light source behind the page that glass panels refract (never put the glow on the card itself — put it on the page background), specular top-edge highlights (1px gradient line, not a uniform border), and grain texture so it doesn't read as a flat digital sheet.

### Color tokens
```css
--bg: #08080a;              /* near-black page base, never pure #000 */
--silver: #eceaf0;           /* the one accent — bright numbers, live states */
--silver-dim: #9c9aa5;       /* secondary emphasis, gradient stops */
--text: #f2f1f5;             /* primary text */
--text-dim: #86848f;         /* labels, secondary text */
--text-faint: #48474e;       /* captions, tick marks, disabled */
--glass-border: rgba(255,255,255,0.07);
--glass-border-strong: rgba(255,255,255,0.14);
```
No color-coding for good/bad (no green/red). Monochrome is the whole point — positive/negative and status signal has to come from typography weight, icon direction (↑/↓), and position, not hue. **Resolved in Phase 3**: icon direction + weight + border-strength, no hue. See Build Log.

### Typography
- **Display (headings, section titles):** Clash Display, weight 500–600 — loaded via Fontshare CDN (`api.fontshare.com`), not self-hosted (no font files were provided).
- **Body/UI (labels, copy):** General Sans, weight 400–600 — same Fontshare load.
- **All numbers, everywhere — no exceptions:** IBM Plex Mono, via `next/font/google`. Tabular figures, financial-terminal feel.

### Glass panel spec
```css
.glass{
  background: linear-gradient(155deg, rgba(255,255,255,0.055), rgba(255,255,255,0.012) 40%, rgba(255,255,255,0.025));
  backdrop-filter: blur(20px) saturate(120%);
  border: 1px solid var(--glass-border);
  border-radius: 20px;
}
/* + specular top highlight (::before, 1px gradient line) */
/* + faint corner glow (::after, radial gradient, blurred) */
```
Page background carries 3 layered radial-gradient light sources (top-left, top-right faint, bottom-center) at low opacity. Grain overlay via SVG feTurbulence at ~5% opacity, `mix-blend-mode: overlay`, fixed position.

### Layout
Bento grid, not a uniform card grid — vary panel size by data importance (Current Balance is the largest panel on Command Deck, always). 12-column grid, gap 16px, 20px border-radius on all glass panels.

### Signature element
The Cadet Week progress bar is not a generic progress bar — it's light traveling through liquid glass toward Fall Finals, with an animated sweep highlight (`prefers-reduced-motion` disables the sweep). Reused in Semester Planner.

### What NOT to do
- No color-coded charts/badges (breaks monochrome discipline)
- No flat single-layer blur — always layer at least 2 glass densities per screen
- No pure black backgrounds, no pure white text
- Don't let hierarchy collapse to "everything is silver" — text-dim/text-faint need to be doing real work

## 3. Data model (Supabase tables)

**Ground truth is the real `Expense_Tracker.xlsx`, not this section verbatim** — see Build Log for the corrections that were made (no Payoneer balance account, categories carry a `monthly_budget`, exact enum values, etc.) before writing the actual migration in Phase 2.

```sql
-- Single settings row (fx rate, matriculation date, starting balances)
settings (
  id, fx_rate_etb_per_usd, matriculation_date,
  sofi_starting_balance, ally_starting_balance,
  cash_starting_balance, updated_at
)

categories (
  id, name, monthly_budget, sort_order
)

transactions (
  id, date, cadet_week int,          -- computed on insert from matriculation_date
  category text,                      -- CHECK constraint, 10-value enum, see §4
  description text,
  necessary_or_discretionary text,    -- 'Necessary' | 'Discretionary'
  is_recurring boolean,
  currency text,                      -- 'USD' | 'ETB'
  amount_original numeric,
  amount_usd numeric,                 -- computed at insert time using fx_rate at that date
  payment_method text,
  notes text
)

income (
  id, date, cadet_week int, source text,
  currency text, amount_original numeric, amount_usd numeric, notes text
)

recurring_bills (
  id, name, category, monthly_cost_usd, billing_day, payment_method, active boolean
)

savings_goals (
  id, name, target_amount_usd, target_date, saved_so_far_usd
  -- remaining, % complete, monthly $ needed = computed, not stored
)

net_worth_snapshots (
  id, snapshot_date, sofi_actual, ally_actual, cash_actual, notes
  -- total_actual, computed_balance, variance = computed
)

key_dates (
  id, event, window_label, status, budget_note
  -- "window_label" is free text (e.g. "Late Nov 2026") — most real dates are estimated ranges, not exact ISO dates.
  -- Named window_label, not window, because WINDOW is a reserved word in Postgres.
)
```

**Category enum** (from Monthly Rollup headers, real xlsx order): Mess Hall/Dining, Uniform/Gear/Laundry, Personal Care, Weekend/Leave/Travel, Academic/Books, Transportation, Health/Medical, Tech & Subscriptions, Gifts & Occasions, Other.

**Payment methods** (real xlsx Settings tab): SoFi Debit, Ally, Payoneer, Cash, VMI Cadet Store Charge, Other. Payoneer is a payment method only — it does **not** have its own balance/starting-balance field.

**Income sources** (real xlsx, not a hard constraint — free text with UI suggestions): Family Support, Cadet Pay/Stipend, SoFi/Ally Drawdown.

**Cadet Week logic:** `floor((date - matriculation_date) / 7) + 1`, matching the Weekly Rollup sheet. Dates before matriculation are negative weeks (pre-departure spend).

## 4. Pages / features (full parity — build in this order)

1. **Auth** — login screen, gated app, no public routes except `/login`.
2. **Command Deck (`/`)** — the home dashboard. Big stat cards: This Month Spent, This Month Income, Current Balance, Monthly Recurring Burn, Total Saved, Discretionary %, Semester Status. Plus (found in real xlsx, not in original written spec): **Budget vs Actual — This Month** and **Life-to-Date Spend by Category** panels, both per-category. Pull live from computed queries, not stored snapshots.
3. **Quick Add (mobile-first component, available from every page)** — floating action button → transaction or income entry in ≤4 taps. Currency toggle (USD/ETB) auto-converts using current settings FX rate.
4. **Transactions** — full table, filterable by category/date range/necessary-discretionary, editable inline, CSV export.
5. **Income** — same pattern as Transactions.
6. **Weekly Rollup** — table + line chart, computed live from transactions grouped by cadet_week.
7. **Monthly Rollup** — table + stacked bar (category breakdown per month) + running balance line.
8. **Recurring Bills** — CRUD list, shows total monthly recurring burn, flags upcoming billing dates.
9. **Savings Goals** — progress bars, computed remaining/monthly-needed.
10. **Net Worth Tracker** — snapshot log + variance vs. computed balance (catches tracking errors early).
11. **Semester Planner** — Fall 2026 / Spring 2027 cards, budget pacing (elapsed % vs spend %), status badge (On Pace / Over Pace / Under Pace).
12. **Insights** — life-to-date totals, avg daily/weekly spend, days since matriculation — all computed, no manual entry.
13. **Key Dates** — leave/furlough calendar with budget notes, static-ish table with edit capability.
14. **Settings** — FX rate, matriculation date, starting balances. Changing FX rate should NOT retroactively rewrite historical `amount_usd` values (each transaction locks in the rate at entry time via a `BEFORE INSERT` trigger).

## 5. Data migration
One-time script pulling rows out of `Expense_Tracker.xlsx` Transactions/Income/Recurring Bills/Savings Goals/Net Worth/Key Dates tabs into the new tables, skipping the literal "example row — delete or overwrite" placeholder rows. As of the Phase 1/2 build, the real workbook's Transactions and Income tabs contain **only** that placeholder row — nothing real to migrate yet. Real reference data that **is** worth seeding directly (not placeholders): Settings (FX 180, matriculation 2026-08-15), 10 categories, Fall/Spring semester windows, all 10 Key Dates rows, the one real Recurring Bill ("Claude," $20/mo), and the Day-1 Net Worth baseline snapshot.

## 6. Explicit non-goals for v1
- No multi-user support, no sharing/permissions
- No bank account sync (Plaid, etc.) — manual entry only, matching current workflow
- No native mobile app — responsive PWA-feel web app is enough

## 7. Build log / session decisions

This section is the persistent record of decisions made while actually building this (vs. the original written spec above, which is now historical intent). Read this before starting any new phase.

- **Supabase project:** `xgmznitqwcvjgeoyghor` (dashboard name "Bigboy money count", org `qgpwmwhoqnyhyomlqnkb` / account `a2020202432@gmail.com`) is authoritative. It was initially invisible to the Supabase MCP connector (which was scoped to a different org, `systemsbyabel.com` / `bgdzmiamjgnmvudvoxny`, holding unrelated projects like `vmi-expense-tracker` — not used, do not touch). Abel reconfigured the Supabase connector itself so it now points at the right account; `mcp__Supabase__list_projects` shows only `xgmznitqwcvjgeoyghor` as of Phase 2.
- **DB access:** resolved — the Supabase MCP connector now has direct access to `xgmznitqwcvjgeoyghor`. If a future session finds `list_projects` doesn't show it, that means the connector's scope changed again; ask Abel to check the Supabase dashboard's org switcher.
- **Auth:** originally Abel created the one auth user himself in the Supabase dashboard; superseded in the post-launch signup-gate change below — account creation now happens in-app at `/login`, still exactly one account, no open public signup.
- **Next.js 16 specifics:** use `proxy.ts` (not `middleware.ts`), exported function name `proxy`. `cookies()`/`headers()`/`params`/`searchParams` are all async — always `await` them. `next lint` is removed; lint via the `eslint` CLI (`npm run lint`, already wired in `package.json`). Turbopack is the default bundler.
- **Supabase SSR pattern:** `lib/supabase/client.ts` (browser), `lib/supabase/server.ts` (server, async `cookies()`), `lib/supabase/proxy.ts` (`updateSession`, uses `supabase.auth.getClaims()` — never trust `getSession()` on the server), root `proxy.ts` wires it up with a matcher excluding static assets.
- **Schema corrections from the real xlsx** (overriding the original spec's data model): no `payoneer_starting_balance` / `payoneer_actual` anywhere — SoFi/Ally/Cash are the only balance-holding accounts, Payoneer is payment-method-only. Categories are a real lookup table with `monthly_budget`, not a bare enum, because Command Deck's "Budget vs Actual" panel needs it. `key_dates.window` is free text, not a clean start/end date pair, because most real rows are "Estimated" fuzzy ranges.
- **Fonts:** IBM Plex Mono via `next/font/google`; Clash Display + General Sans via Fontshare CDN link tags in `app/layout.tsx` `<head>` (matches `command-deck-mono.html` exactly, no font files were provided to self-host).
- **Phase 1 status:** scaffold complete — Next.js 16 + Tailwind v4 + Supabase SSR auth (`proxy.ts` gate) + full design token system in `app/globals.css` + all 12 route shells + nav shell (`components/nav/AppShell.tsx`) + Quick Add FAB shell (`components/QuickAddFab.tsx`, real logic lands in Phase 3) + a fully-styled static Command Deck (`app/(app)/page.tsx`) built directly against `command-deck-mono.html`, including the Budget-vs-Actual and Life-to-Date panels not in the original written spec. Placeholder values throughout.
- **Phase 2 status:** schema live on `xgmznitqwcvjgeoyghor` — 9 tables (`settings`, `categories`, `semesters`, `transactions`, `income`, `recurring_bills`, `savings_goals`, `net_worth_snapshots`, `key_dates`) with RLS (`authenticated`-only, single policy per table — no `user_id` column, genuinely single-user), a `set_computed_fields()` trigger locking in `cadet_week`/`amount_usd` on insert/update of `transactions`/`income` using the settings row active at that moment, and 9 computed views (`account_balance`, `budget_vs_actual_this_month`, `life_to_date_spend_by_category`, `weekly_rollup`, `monthly_rollup`, `monthly_category_totals`, `savings_goal_progress`, `net_worth_variance`, `semester_pacing`) all forced to `security_invoker = true` so they respect the querying user's RLS rather than the view owner's. Real reference data seeded: settings (fx 180, matriculation 2026-08-15), 10 categories (budgets all $0, Abel hasn't set them yet), Fall/Spring semester windows, all 10 Key Dates rows, the "Claude" recurring bill, and the Day-1 net worth baseline. `transactions`/`income` are empty (only placeholder rows existed in the source xlsx). Raw SQL lives in `supabase/migrations/` for the record; `lib/supabase/database.types.ts` has the generated types wired into both Supabase clients. Ran `get_advisors` after every DDL change — the only remaining warnings are the expected "RLS policy always true" ones (correct for single-user) and a pre-existing `rls_auto_enable()` platform function not created by this app.
- **Phase 3 status:** Command Deck fully wired to live Supabase queries/views (no more placeholders); Quick Add is a real working form (transaction/income toggle, category picker, USD/ETB conversion preview) backed by server actions. Color-signal question resolved: icon direction + badge weight only, no hue.
- **Phase 4 status:** Transactions and Income are full CRUD tables — filters, inline edit (server actions bound per-row id), delete, client-side CSV export matching the real xlsx column layout.
- **Phase 5 status:** Weekly Rollup (cash-flow line chart + running-balance line chart, kept as two charts since the two metrics live on very different scales — no dual-axis) and Monthly Rollup (stacked category bar chart + running-balance line) both built with Recharts, following the project's `dataviz` skill. The 10-category stacked bar uses a validated monochrome grayscale ramp (`lib/chart-colors.ts`) since hue is off-limits — it clears the contrast floor but lands just under ideal adjacent-lightness separation (a real limit of 10 pure-gray slots), so it leans on a legend, 2px segment gaps, hover tooltips, and the table underneath rather than asking color alone to carry identity.
- **Phase 6 status:** Recurring Bills, Savings Goals, Net Worth Tracker, Semester Planner, Insights, Key Dates, and Settings are all built out with real CRUD/computed views. Insights has no dedicated Phase 2 view — its figures (life-to-date totals, averages, highest category/week, month-over-month, projected end-of-semester balance) are computed server-side directly in `app/(app)/insights/page.tsx`.
- **Phase 7 status:** `scripts/migrate-xlsx.mjs` (run via `npm run migrate:xlsx -- path/to/file.xlsx [--dry-run]`) imports Transactions/Income/Savings Goals only — Recurring Bills, Net Worth, and Key Dates already got their real rows seeded directly in Phase 2, so re-running an importer against those tabs would duplicate them. It signs in with real Supabase Auth credentials (prompted interactively) rather than a service_role key, since RLS requires an authenticated session for both reads (category lookups) and writes — `--dry-run` still signs in for accurate category-match previews, it just skips the final insert calls. Verified against the real `Expense_Tracker.xlsx`: correctly detects and skips all 3 placeholder rows (Transactions, Income, Savings Goals example rows), so there is currently nothing to import — the script is ready for whenever Abel has real historical entries to bring in.
- **Phase 8 status:** cross-page consistency checked (every page uses `PageHeader`/`Glass` except Command Deck, which intentionally keeps its own richer hero treatment matching the mockup). Accessibility audit found `text-faint` (2.18:1 contrast, fails WCAG AA) had been overused across Phases 4-6 for real functional content — table headers, body data, form labels — instead of its spec-scoped role ("captions, tick marks, disabled"); promoted to `text-dim` (5.44:1, clears AA) everywhere except the one genuinely correct use (Cadet Week progress bar tick captions). Focus-visible outlines and `prefers-reduced-motion` were already handled in Phase 1. No color-hue violations found (grepped for `text-red`/`bg-green`/etc. and stray hex codes — only the ring-gradient's two neutral grays, which match the original mockup).
- **Deployment:** no Vercel action taken — production deploy / `finance.abelatnafu.com` DNS is Abel's call, not something to do unilaterally. A Vercel project named `final-comand-center` already exists and auto-deploys previews on push via its GitHub integration (confirmed working across every phase's push, including this one).
- **Post-launch: in-app account creation.** Abel asked to create/sign in from the app itself instead of the Supabase dashboard. Since the RLS model here is deliberately single-tenant (`using (true)` for any `authenticated` user — see Phase 2 note above), a normal open signup route would let anyone who finds the URL create an account and get full read/write access to all of Abel's financial data. Resolved by adding a **self-disabling signup gate**: a `public.can_create_first_account()` Postgres function (`security definer`, `supabase/migrations/20260711100000_first_account_signup_gate.sql`) that returns `not exists (select 1 from auth.users)` — true only until the first account exists, then permanently false. `/login` (`app/login/page.tsx`, now an async server component) calls it via RPC and only renders the "Create your account" link/form (`components/LoginForm.tsx`) when it's true; the `signUp` server action (`app/login/actions.ts`) re-checks the same gate server-side right before calling `supabase.auth.signUp()`, so hiding the UI isn't the only thing standing between the internet and a second account. `get_advisors` flags this function as anon-executable `security definer`, which is intentional and expected — it only ever returns a boolean, never row data. Once Abel signs up once, the create-account path disappears everywhere (client hides it, server rejects it) and `/login` behaves exactly as before. Whether Supabase requires email confirmation before the new session is active depends on the project's Auth settings (not changed by this work); if so, the UI shows a "check your email" state after signup instead of redirecting straight in. Account creation later moved from a client-side toggle on `/login` to its own `/signup` route with a shared `AuthTabs` Log In / Sign Up switcher on both pages (same gate, same lockout — just a more visible entry point).
- **Neo-Luxury redesign (post-launch, superseded — see "Apple/iOS redesign" below).** Abel asked to drop Liquid Glass Monochrome entirely and go "one of one" — a full visual identity swing, not a tweak. Explored via `AskUserQuestion`: direction = **Neo-Luxury** (ink, brass, ledger-page feel — private-banking, not fintech-app), workflow = show a mockup before touching code. First attempt was a real Figma file (`GkDY96vnCG4LotTcWU07UM`), but Abel's Figma seat (Starter plan, View seat) caps MCP tool calls at 6/month and we hit that limit mid-skeleton — pivoted to an HTML preview (published as a Claude Artifact) for the approval step instead, then implemented the approved direction directly in code once Abel said go.
  - **Palette** (`app/globals.css` `:root`): `--bg: #0b0908` (warm near-black, not the old cold `#08080a`), `--ink-raised: #17120d` (card surface), `--brass-dim: #8a723c` / `--brass: #c9a24b` / `--brass-bright: #e3c27e` (three-tone brass, was two-tone silver), `--ledger: #f3ede3` / `--ink-on-ledger: #2a2119` (the one inverted-card treatment, see below), `--text: #f3ede3` (ivory) / `--text-dim: #a99c86` (taupe) / `--text-faint: #5c5347`. `--glass-border`/`--glass-border-strong` are now brass-tinted rgba instead of white. Tailwind-exposed tokens are `--color-brass` (→ brass-bright, replaces `text-silver`) and `--color-brass-dim` (→ brass); every `text-silver`/`bg-silver`/`accent-silver` usage app-wide was renamed to `-brass`.
  - **Type**: swapped Fontshare (Clash Display + General Sans) for `next/font/google` entirely — **Fraunces** (display; also now carries `.stat-value`/`.hero-value`, the big headline figures) and **Manrope** (body/UI), both self-hosted via Next the same way IBM Plex Mono already was. IBM Plex Mono is now scoped specifically to tabular data (the `.num` class, table cells) rather than every number — headline figures get serif treatment on purpose (old-ledger vs. trading-terminal is the whole tension of the concept).
  - **`.glass`**: no more blur/frost at all — solid `--ink-raised` surface, brass hairline border, `border-radius` down from 20px to 3px (architectural, not soft), `::before` is now a brass "gilt edge" line (was the white specular highlight) — same structural idea, new material. `::after` corner glow was dropped (didn't suit a non-blurred surface). `.glass-dense` similarly lost its white tint for a plain dark inset.
  - **The one deliberate risk**: Command Deck's Current Balance panel is no longer a `Glass`/`.glass` card — it's a new `.ledger` class (`app/globals.css`), a warm ivory "ledger page" (ruled-line pseudo-element background, a brass wax-seal medallion top-right) sitting on the dark page, built directly in `app/(app)/page.tsx` (bypassing the shared `Glass` component on purpose, same as that page already did pre-redesign). Its label/value colors are set inline in that one file, not via the shared `text-dim`/`text-brass` tokens, since they need to read on a light card in an otherwise all-dark app.
  - **Signature motif**: Cadet Week / Semester Planner / Savings Goals progress bars (`.liquid-track`/`.liquid-fill` — class names kept, restyled) went from a 14px pill to a 4px engraved brass ruler line, gradient `brass-dim → brass-bright`, same reduced-motion-respecting sweep animation reinterpreted as light catching metal instead of refracting through glass. Command Deck's version additionally got a row of 9 tick marks between the bar and the milestone labels (new markup, `app/(app)/page.tsx`), matching the approved mockup.
  - **Charts** (`lib/chart-colors.ts`): `CATEGORY_SHADES` is now a warm brass-tinted 10-step neutral ramp (was cold gray) — still monochrome-family by design, just re-hued; line/grid/axis colors follow the same brass/taupe swap. Hardcoded `#08080a` background-matching strokes in the Recharts components were updated to `#0b0908`.
  - **Buttons/inputs**: `border-radius` down to 2–4px across `.btn`/`.input`/`.select` (was 10–999px pill), `.btn-primary` is now a brass gradient with ink text (was silver-on-nothing).
  - Every `border-white/[…]`/`bg-white/[…]` opacity utility across the table/nav components (row dividers, hover states, sticky-column backgrounds) was swapped for the brass-tinted equivalent — grepped the whole `app`/`components` tree to confirm zero white-opacity or stray-hex leftovers from the old system before calling this done.
  - No color-coding for good/bad is still the rule — that discipline survives the redesign unchanged, it just was never really about silver vs. brass.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean; screenshotted `/login` via a headless-Chromium dev-server render (the one page whose shell doesn't require live Supabase data, which this sandbox can't reach) to visually confirm fonts/palette/card treatment actually render as designed, not just compile.
- **Apple/iOS redesign (post-launch, supersedes Neo-Luxury).** Abel asked for it to feel like Apple made it — an iOS/Apple HIG identity, not another bespoke one-off. This one didn't need an `AskUserQuestion` round first: unlike "one of one" (genuinely open-ended taste) or "Neo-Luxury" (a coined direction needing a definition), "Apple/iOS feel" is a real, well-documented design language (Apple's Human Interface Guidelines) with known, specific conventions — so it was implemented directly. Same token-first strategy as the two prior redesigns, so most components re-themed automatically through `app/globals.css` and `lib/chart-colors.ts`.
  - **Same turn, Abel also flagged "you removed the create account page" — investigated before touching anything: nothing was removed.** `app/signup/page.tsx`/`SignupForm`/`AuthTabs` are untouched since they were built; `select public.can_create_first_account()` confirmed it now returns `false` because `auth.users` has exactly 1 row (Abel's own account, created earlier). The self-disabling gate is doing exactly its job — `/signup` correctly redirects to `/login` and the Sign Up tab correctly hides once the one account exists. No code change needed; explained this to Abel with the query result as evidence rather than reopening signup, since that would undo the security work from the earlier post-launch entry above without him actually asking for a second account.
  - **Palette** (`app/globals.css` `:root`): true black `--bg: #000000` (iOS's actual OLED dark-mode base — deliberately breaks this project's earlier "never pure black" rule from every prior design system, because true black genuinely is the correct, recognizable iOS choice here), `--surface: #1c1c1e` / `--surface-secondary: #2c2c2e` (real iOS systemGray6/systemGray5 dark values), `--accent: #0a84ff` (iOS systemBlue, dark-mode variant — the one accent, replacing brass), `--text-dim`/`--text-faint` are `rgba(255,255,255,0.6)`/`rgba(255,255,255,0.3)` rather than distinct hex — this matches Apple's own documented opacity-based label hierarchy (`secondaryLabel`/`tertiaryLabel`) exactly, not an approximation of it. Tailwind tokens renamed `--color-brass`/`--color-brass-dim` → `--color-tint`/`--color-tint-dim` ("tint color" is Apple's own term for this exact concept) rather than leaving the old name pointing at a new color — learned that lesson from the `silver`→`brass` rename during the Neo-Luxury pass. Every `text-brass`/`accent-brass` usage app-wide renamed to `-tint`.
  - **Type**: dropped `next/font/google` entirely — no self-hosted display/body typeface anymore. Uses the real system font stack (`-apple-system, BlinkMacSystemFont, "SF Pro Display"/"SF Pro Text", …` and `ui-monospace, "SF Mono", …`), which resolves to actual SF Pro/SF Mono on Apple devices for free and falls back sanely elsewhere — this is itself an authenticity choice, since native Apple apps don't download a custom typeface either. `app/layout.tsx` is now font-import-free. Removes the whole "does this weight exist in the loaded set" bug category from the Neo-Luxury pass, since system fonts carry their full native weight range.
  - **`.glass` material**: blur is back, but calibrated to Apple's own documented vibrancy formula this time (`backdrop-filter: blur(20px) saturate(180%)` over `rgba(28,28,30,0.72)`), not the earlier ad-hoc gradient-tint approach from Liquid Glass Monochrome. `border-radius` up to 16px (was 3px under Neo-Luxury) — iOS cards read as soft continuous-corner rects, not architectural hairline panels. Command Deck's hero (`.ledger` class, name kept for continuity even though the ivory-inverted-page concept is gone) is the same material with a blue-tinted gradient instead of the flat surface color.
  - **Nav is now structurally different, not just reskinned**: mobile went from a hamburger-menu dropdown to a real iOS bottom tab bar (`components/nav/AppShell.tsx`) — 3 primary destinations (Command Deck, Transactions, Income) plus a "More" tab that opens a bottom sheet listing the other 9 routes, matching the exact pattern iOS apps use once they exceed 5 tab items. New hand-authored SF-Symbols-style line icons (`components/nav/icons.tsx` — house, list, dollar-in-circle, ellipsis-in-circle; simple stroke paths, not traced/complex, screenshotted standalone to verify they render clean before wiring in). `QuickAddFab`'s FAB moved up (`bottom-24` mobile) so it doesn't collide with the new tab bar.
  - **Segmented controls**: the transaction/income toggle in Quick Add and the Log In/Sign Up switcher (`AuthTabs`) were both upgraded from two separate pill buttons to a single genuine iOS `UISegmentedControl`-style container — one rounded-rect track, a lighter "thumb" behind the selected option.
  - **Signature motif**: Cadet Week progress bar dropped the engraved-ruler tick marks and sweep-glint animation entirely in favor of a flat, calm 6px systemBlue progress bar — iOS progress views don't shimmer or glint (that skeuomorphic-adjacent detail reads as pre-iOS-7 Apple, not current).
  - **Charts** (`lib/chart-colors.ts`): category ramp is now a true neutral iOS systemGray scale (grays only, no warm tint), line colors are systemBlue/systemCyan. Discretionary-spend ring on Command Deck recolored to a systemBlue gradient (this is deliberately evocative of Apple Watch/Fitness Activity rings, the single most recognizable "Apple made this" UI motif available for a single-ring context).
  - **Buttons/inputs**: `border-radius` up to 10px (rounded rect, not the Neo-Luxury architectural 2–4px); `.btn` is now iOS's "tinted" borderless style (translucent blue fill, blue text) rather than an outlined button, `.btn-primary` is solid systemBlue.
  - No color-coding for good/bad is **still** the rule, carried through a third design system unchanged — icon direction (↑/↓) and weight still do that job, not hue, even though this app now uses a blue accent instead of monochrome-only. (Apple's own Stocks app would use red/green here; deliberately didn't, to keep the one restraint this project has held onto across three total redesigns.)
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean; grepped the whole `app`/`components` tree for old-palette hex/rgba/class-name leftovers before calling it done (same discipline as the Neo-Luxury pass); screenshotted `/login` at both desktop and iPhone viewport widths, plus the four new nav icons rendered standalone outside the app shell (since the app shell itself needs a live authenticated session to reach, which this sandbox can't do) to visually confirm the actual pixels, not just that it compiles.
  - **Deep-detail "make it exactly Apple" polish pass (same design system, tightened to real HIG values).** After the first Apple pass Abel asked to go top-to-bottom and make it perfect. `app/globals.css` was rebuilt against the actual HIG spec rather than approximations: full systemGray scale (`#8e8e93`…`#1c1c1e`), tinted-white label hierarchy (`--label`/`--label-secondary rgba(235,235,245,0.6)`/`tertiary 0.3`/`quaternary 0.18`), real separator (`rgba(84,84,88,0.65)`) and system fill tiers, and the complete SF **text-style ramp** as classes (`.ios-large-title` 34/700, `.ios-title2` 22/700, `.ios-title3`, `.ios-headline` 17/600, `.ios-body`, `.ios-subhead` 15, `.ios-footnote` 13, `.ios-caption`, `.section-header`). Key corrections from the first pass:
    - **Material vs. solid split** — the first pass blurred *every* card, which isn't how iOS works. Now `.glass` is a **solid** grouped-list card (`--bg-elevated #1c1c1e`, 16px radius, no border — the Settings/Health/Stocks look), and blur/vibrancy (`.material`) is reserved for **overlay chrome only** (bottom tab bar, Quick Add sheet, More sheet), which is the authentic iOS distinction.
    - **Money is no longer SF Mono** — `.num` was switched from `--font-mono` to the body SF stack with `font-variant-numeric: tabular-nums` + `font-feature-settings: "tnum"`. SF Mono is for code; iOS shows currency in tabular SF Pro. Chart axis labels moved off mono too.
    - **Real iOS switch** — the two boolean checkboxes (Quick Add "Recurring", Recurring Bills "Active") are now a `.ios-switch` (native checkbox restyled to the 51×31 track with a shadowed knob) that is **systemGreen when on** — the signature iOS toggle color, not blue.
    - **Real segmented control** — `.segmented` is now a proper `UISegmentedControl`: a `--fill-quaternary` track with a lighter `--gray2` sliding thumb carrying a soft shadow, driven by `data-active`. Used by Quick Add (Expense/Income) and `AuthTabs`.
    - **Blue is reserved for interactive** — running-balance / total values in the rollup + net-worth tables were de-emphasized from `text-tint` (blue) to `text-text font-semibold`, since in iOS blue means "tappable." Row actions became `.link-action` (blue Edit) and `.link-destructive` (**systemRed** Delete) — destructive-in-red is a core HIG convention this app had been ignoring.
    - **Large-title navigation** — `PageHeader` and Command Deck now render a true 34px bold large title (the mono uppercase "eyebrow" kicker with the glowing dot is gone); the redundant mobile top app-bar was removed since the in-content large title is the header (iOS pattern). Command Deck's Budget/Life-to-Date panels became real **inset grouped lists** (`InsetList` — hairline separators inset from the leading content edge). Progress bar lost its tick marks (iOS doesn't tick a progress view).
    - Verified with `tsc`/lint/build clean, a full grep sweep (only iOS-scale hex + consistent radii remain), a standalone full-system harness screenshot (rendering the real component CSS: large title, balance hero, tinted badge, systemBlue ring, inset list, segmented control, green switches, tinted/filled buttons, progress bar, tab bar all together) plus the live Tailwind-compiled `/login` at iPhone width.
- **Light-mode "Apple flare" dashboard redesign (post-launch, supersedes the dark iOS pass above; from reference screenshots).** Abel shared three reference shots of a premium **light** Apple finance dashboard ("it still doesn't have an Apple flare … redesign the whole thing make it look like that") and, mid-turn, asked to add liquid-glass where warranted, a **theme switch**, a **sidebar in Settings**, and a **bubbly Apple date picker** to replace the native one. This is the fourth full design swing; same token-first strategy so most components re-themed through `app/globals.css`.
  - **Default appearance flipped to light.** `:root` is now the light system (`--bg #f5f5f7` page, `--bg-elevated #ffffff` cards, `--bg-elevated-2 #f2f2f7`, `--bg-sidebar #fbfbfd`), full light systemGray scale, near-black `--label #1d1d1f` with the documented `secondaryLabel rgba(60,60,67,0.6)`/`tertiary 0.3`/`quaternary 0.18` opacity hierarchy, `--blue #007aff`, and — **breaking this project's long-standing "no color-coding for good/bad" rule for the first time in four redesigns** — real semantic **green `#34c759` / red `#ff3b30`** for income/expense, because the reference leans on them and Abel explicitly wanted that look. `.pos`/`.neg` helpers added; cards carry a soft two-layer `--card-shadow` + hairline border instead of blur.
  - **Theme system (Light / Dark / Auto).** `components/ThemeToggle.tsx` is a segmented control writing `document.documentElement.dataset.theme` + persisting to `localStorage("theme")` (absent = follow OS). Dark appearance lives in `globals.css` as both `:root[data-theme="dark"]{…}` (explicit) and `@media (prefers-color-scheme: dark){ :root:not([data-theme="light"]){…} }` (Auto) — true-black `#000`, elevated `#1c1c1e`, dark systemGray + brighter system colors (`--blue #0a84ff`, green `#30d158`, red `#ff453a`). Themeable indirection tokens (`--material-bg`, `--hero-bg`, `--badge-bg`/`--badge-fg`) let the frosted overlays, dark hero, and badges flip correctly. A tiny inline **no-flash init script** in `app/layout.tsx <head>` resolves the saved theme before first paint (`suppressHydrationWarning` on `<html>`).
  - **Liquid glass, used with restraint.** `.material` (`backdrop-filter: blur(24px) saturate(1.8)` over the themeable `--material-bg`) is reserved for overlay chrome (desktop top bar, mobile tab bar, Quick Add sheet, More sheet) and the date-picker popover — not every card, matching real iOS material vs. solid-list distinction.
  - **Bubbly Apple date picker** (`components/ui/DatePicker.tsx`) replaces every native `type="date"` input app-wide (Quick Add, Transactions edit + From/To filters, Income edit + filters, Net Worth, Savings Goals, Settings matriculation). It's a calendar-popover: rounded pill day cells with `active:scale(.9)`, a solid-blue selected day, blue-bold "today", muted adjacent-month days, prev/next month chevrons, Today/Clear actions. Renders through a **`createPortal(document.body)` + fixed positioning** so `overflow:hidden` on `.glass` cards and `backdrop-filter` containing-blocks can't clip it; closes on outside-click / Escape / scroll / resize. Supports controlled (`value`/`onChange`, for filters) and uncontrolled (`name`/`defaultValue` + hidden input, for server-action forms) use; ISO strings parsed manually to dodge UTC off-by-one.
  - **Settings gets a sub-nav sidebar** (`components/settings/SettingsShell.tsx`): a sticky left rail with Appearance / Financial / Account rows (colored rounded-square icons, active row solid-blue). Appearance hosts the ThemeToggle; Financial holds the existing FX/matriculation/balances `SettingsForm`; Account shows the signed-in email + a red destructive Sign out (server action passed through from the app layout). Stacks/scrolls horizontally on mobile.
  - **Rich dashboard + charts** (from the earlier part of this same turn): Command Deck rebuilt into the reference's layout — a dark Net-Worth **hero card** with a purple→blue gradient `AreaChart` and a 1M/6M/1Y/ALL segmented range toggle (`components/dashboard/NetWorthHero.tsx`), a green/red Cash-Flow `BarChart`, a spending donut with center label, colored account rows (`components/ui/AppIcon.tsx`), upcoming payments, insights, and savings mini-rings. Recharts lives in `components/charts/DashboardCharts.tsx`. AppShell became a light sidebar (VMI Finance wordmark + `NavIcon` rows + dark promo card) with a desktop top bar and the iOS bottom tab bar / More sheet on mobile.
  - **Identity kept as VMI Finance** — deliberately no Apple logo/wordmark/"© Apple" anywhere; only the *design language* is borrowed, since impersonating Apple in a real deployed product isn't OK.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean; grep sweep confirmed zero native `type="date"` inputs and no old-palette (`brass`/`silver`) leftovers remain; standalone harness (real stripped `globals.css`) screenshotted in **both light and dark** — settings sidebar, theme segmented control, open bubbly calendar, dark hero, and green/red money cards all render and flip correctly. The two `set-state-in-effect` lint hits (ThemeToggle localStorage read, DatePicker portal mount-gate) are the project's already-accepted external-sync pattern, suppressed inline like `QuickAddFab`.
  - **Follow-up: more settings + a Liquid-Glass amount slider.** Abel asked to "add more things to the settings and a slider that controls the amount of liquid glass." Appearance section (`components/settings/AppearanceSettings.tsx`) grew from just the theme switch to four grouped cards: **Theme**, **Liquid Glass** (a range slider driving a new `--glass-blur` token, 0–40px, that `.material` and `.cal-pop` now read — so the frosted tab bar/sheets/popover blur is user-dialable; includes a live frosted-preview swatch), **Accent Color** (six iOS swatches overriding `--blue`/`--blue-hover` inline on the root, which propagates through `--color-tint` to every button/link/selected state), and **Reduce Motion** (an `.ios-switch` setting `data-reduce-motion` on the root, matched by a new forced-reduced-motion CSS block mirroring the `prefers-reduced-motion` one). All three new prefs persist to `localStorage` (keys `glass`/`accent`/`reduceMotion`) and are re-applied by the extended **pre-paint init script** in `app/layout.tsx` (the accent name→hex map is duplicated there and in the component, kept in sync). Financial section gained a **Category Budgets** editor (`components/settings/CategoryBudgetsForm.tsx` + `updateCategoryBudgets` server action) — an inset list of the 10 categories with a `$` input each and a live monthly total, writing `categories.monthly_budget` (which had been seeded at $0), directly feeding Command Deck's Budget-vs-Actual panel. `SettingsShell` now takes `categories`; the settings page loads them. `.glass-slider` is a custom-styled range input (thin track, blue fill via inline gradient, white shadowed thumb). Verified tsc/lint/build clean + harness screenshots of the full Appearance panel and Budgets list in both light and dark.
- **Multi-tenant conversion + open signup (the app is no longer single-user).** Abel asked "can we make it so its not just made for me and more users can use it, and add a create a sign in page for new user." This reverses the deliberate single-tenant decision from the post-launch signup-gate entry above — confirmed with him first via `AskUserQuestion` on two points, both answered "recommended": (1) **fully open signup**, no invite/allowlist gate, and (2) **generalize the VMI-cadet-specific terminology** rather than leave it as-is for other users.
  - **Real per-user data isolation, not just a UI change.** Every table (`settings`, `categories`, `semesters`, `transactions`, `income`, `recurring_bills`, `savings_goals`, `net_worth_snapshots`, `key_dates`, plus two new ones below) got a `user_id uuid not null references auth.users(id) default auth.uid()` column, and every RLS policy was rewritten from the old single-tenant `using (true)` to `using ((select auth.uid()) = user_id)` (the `select`-wrapped form per Supabase's RLS perf guidance — confirmed via `get_advisors` that the unwrapped form re-evaluates `auth.uid()` per row). Abel's existing data (settings, 10 categories, 2 semesters, the "Claude" bill, the Day-1 net worth snapshot, all 10 key dates) was backfilled to his real `auth.users.id` in the same migration, not dropped.
  - **Hardcoded SoFi/Ally/Cash became a real `accounts` table** (`id, user_id, name, starting_balance, sort_order`) — the biggest structural change, since baking Abel's two specific bank names into dedicated `settings` columns (`starting_sofi`/`starting_ally`/`starting_cash`) and `net_worth_snapshots` columns (`sofi_actual`/`ally_actual`/`cash_actual`) would have shown every other user fields literally labeled "SoFi" and "Ally". Abel's own three accounts were migrated in as real `accounts` rows named "SoFi"/"Ally"/"Cash" (the generalization is about not hardcoding those names in the *schema*, not erasing them from *his* data — they're exactly correct as his data). Net worth snapshots gained a child table, `net_worth_snapshot_balances` (`snapshot_id, account_id, amount`, unique on the pair so upserts work), plus a `net_worth_snapshot_detail` view joining it to account names for display — replacing the fixed 3-column snapshot shape with one that scales to however many accounts a user actually has. `NetWorthTable`/`SnapshotForm` now render one input column per account dynamically instead of three fixed ones. New Settings → Financial → **Accounts** card (`components/settings/AccountsForm.tsx` + `addAccount`/`deleteAccount`/`updateAccountBalances` actions) lets anyone add/remove/rename their own accounts and edit starting balances — this didn't exist before since there was nothing to manage when accounts were hardcoded.
  - **Column renames for genericity:** `transactions.cadet_week`/`income.cadet_week` → `week_number` (and the matching index/view column); `settings.matriculation_date` → `tracking_start_date`. The insert trigger (`set_computed_fields()`) and all 9 computed views were rewritten against the new names — the 9 views were dropped and recreated in one migration since Postgres won't let you rename/drop a column a view depends on. Because every view is still `security_invoker = true`, RLS on the underlying tables scopes each view to the querying user automatically — the view SQL itself needed zero explicit `user_id` filters, just the settings/accounts lookups swapped from `where id = 1` to relying on `settings_user_unique` (one row per user) and summing the new `accounts` table instead of three fixed columns.
  - **`payment_method` relaxed from a hardcoded enum to free text.** The old CHECK constraint (`'SoFi Debit' | 'Ally' | 'Payoneer' | 'Cash' | 'VMI Cadet Store Charge' | 'Other'`) was dropped from `transactions`/`recurring_bills`; `PAYMENT_METHODS` in `lib/constants.ts` became generic suggestions (`Debit Card`/`Credit Card`/`Cash`/`Bank Transfer`/`Other`) offered via a `<datalist>` the same way `INCOME_SOURCES` already worked, instead of a `<select>` that would've forced everyone through Abel's specific banks. `INCOME_SOURCES` similarly dropped `"Cadet Pay/Stipend"`/`"SoFi/Ally Drawdown"` for `"Paycheck"`/`"Savings Withdrawal"`.
  - **Semester Planner → Budget Periods.** Abel's two seeded rows ("Fall 2026"/"Spring 2027") stayed his, but the page/nav copy is now generic and it gained real CRUD (`app/(app)/semester-planner/actions.ts` — it had none before, just a read-only display of the seeded table) so a new user isn't stuck looking at an empty page with no way to add their own period. `semesters` kept its table/route name internally (renaming would've meant touching the `semester_pacing` view, `SemesterCard`, and the route path for no functional benefit) but is user-scoped like everything else now.
  - **Auto-provisioning on signup.** A `handle_new_user()` trigger on `auth.users` (the standard Supabase pattern) fires after every signup and seeds a new `settings` row (fx_rate 1, start date = today), 3 starter accounts (Checking/Savings/Cash, all $0), and 10 generic categories (Housing, Food & Dining, Transportation, Personal Care, Entertainment, Shopping, Health & Medical, Subscriptions, Travel, Other) — deliberately *not* seeding semesters/key-dates/recurring-bills, since those were real Abel-specific content, not placeholder defaults. `revoke execute ... from public, anon, authenticated` on the trigger function mirrors the existing `set_computed_fields()` pattern (Postgres fires trigger functions internally regardless of grants, so this only blocks someone calling it directly as an RPC).
  - **Signup fully opened.** `can_create_first_account()` and its trigger/grants were dropped entirely; `/login` and `/signup` no longer call it or conditionally hide the signup tab. `app/login/actions.ts`'s `signUp` lost its server-side re-check gate. This is the one place the old single-tenant security posture *depended* on RLS being correct everywhere else — which is exactly what the rest of this migration was for.
  - **Branding genericized:** page `<title>`/meta description, the sidebar wordmark, footer, and both auth-page headers changed from "VMI Finance" to "Command Deck"; the Command Deck's hardcoded `"Good morning, Abel."` now reads the signed-in user's email local-part; the top-bar avatar initial is computed from the user's email instead of a hardcoded "A". Grepped the whole `app`/`components` tree case-insensitively for `VMI`/`Cadet`/`Matriculation` afterward — zero hits outside historical `CLAUDE.md` build-log prose (which stays, as the record of what was built and why).
  - **Caught by testing, not just code review:** a rollback-only SQL test (`begin; insert into auth.users ...; select ... from settings/accounts/categories where user_id = ...; rollback;` — inserts a throwaway auth user inside a transaction, checks the trigger provisioned everything correctly, then rolls back so nothing persists) found that `settings.id` had lost its `default 1` when the singleton constraint was dropped for multi-tenancy, with nothing put in its place — every new user's signup would have thrown `null value in column "id"` and silently failed to create their account. Fixed by giving `settings.id` a real sequence-backed default. Re-ran the same rollback test after the fix to confirm a clean provision (1 settings row, 3 accounts, 10 categories) before trusting the migration.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean; `get_advisors` (security) shows only the two pre-existing, unrelated items (the platform's `rls_auto_enable()` function, and Supabase Auth's leaked-password-protection toggle — a dashboard setting, not something a migration can flip, worth Abel turning on now that signup is public); confirmed Abel's existing data (1 settings row, 3 accounts, 10 categories, 2 semesters, 1 recurring bill, 1 net worth snapshot with 3 balance rows, 10 key dates) survived the migration under his real `user_id`; grep-swept for every renamed/removed field (`cadet_week`, `matriculation_date`, `starting_sofi/ally/cash`, `sofi_actual/ally_actual/cash_actual`, `.eq("id", 1)`, `can_create_first_account`) — zero remaining references anywhere in `app`/`components`.
- **"Senior accountant" review + 8 accounting-correctness features.** Abel asked what a senior accountant would flag about the app, then asked to implement all 8 of the resulting recommendations. All shipped in one pass:
  - **Fixed a real correctness bug: FX rate was silently re-locking on edit.** `set_computed_fields()` re-looked-up `settings.fx_rate` on every UPDATE, not just INSERT — editing a February transaction's amount in July would silently re-price it at July's rate, breaking the entire "rate locks in at entry time" invariant the trigger exists to enforce. Fixed by adding a `fx_rate_used` column on `transactions`/`income`, set once on INSERT and never touched again on UPDATE (`new.fx_rate_used := old.fx_rate_used`). Verified with a rollback-only SQL test: inserted a transaction at fx_rate 180, changed `settings.fx_rate` to 90, edited the transaction's amount, confirmed `fx_rate_used` stayed 180 and `amount_usd` recalculated against the locked rate, not the new one.
  - **Transfers**: a new `transfers` table (date, from_account_id, to_account_id, amount_usd, notes) for moving money between your own accounts, so people stop faking transfers as an expense-in-one-account + income-in-another (which was inflating both totals and every chart derived from them). New `/transfers` page (`components/tables/TransfersTable.tsx`), a third Quick Add mode (Expense/Income/Transfer segmented control), and — critically — transfers are structurally excluded from every income/expense view since they live in their own table the aggregate views never touch.
  - **Liabilities**: `accounts.kind` (`'asset' | 'liability'`, default asset). `account_balance`, `net_worth_variance`, `weekly_rollup`, and `monthly_rollup` all sign-flip liability balances (`case when kind = 'liability' then -starting_balance else starting_balance end`) so net worth is actually assets minus liabilities instead of just cash-on-hand. Settings → Accounts gained a Type selector and a "Liability" badge; Command Deck's account rows show liabilities with a minus sign and dimmed icon; NetWorthTable's per-account column headers tag liability columns "(liab.)".
  - **Duplicate detection**: `addTransaction`/`addIncome` now check for an existing row with the same date + amount (+ category for transactions, + source for income) before inserting, and return a `duplicateWarning` instead of silently creating a second copy. Quick Add shows an amber warning banner and swaps the submit button to "Add Anyway" — a second submit sets `confirm_duplicate=true` and forces it through. Not a hard block, since real same-day-same-amount purchases do happen — just a "did you mean to do this twice" nudge.
  - **Cash flow forecasting**: Insights gained "Projected Balance — End of This Month" — current balance + (average daily net cash flow × days remaining in month) − (active recurring bills whose billing day hasn't hit yet this month). Distinct from the existing period-end projection (which needs a Budget Period to exist); this one always works off the calendar month.
  - **Financial health ratios**: Insights gained a "Financial Health" section — Savings Rate (life-to-date net ÷ income), Emergency Fund Coverage (current balance ÷ average monthly spend, in months), and Discretionary Spend Trend (this month's discretionary share vs. last month's, as a point delta with an arrow).
  - **Split transactions**: a new `transaction_splits` table (transaction_id, category_id, amount_usd) lets one purchase span multiple categories instead of forcing a single pick or three fake entries. `transactions.category_id` stays as the fallback for non-split transactions; a new `transaction_category_breakdown` view unions "real splits" with "synthesize one row from the transaction itself when no splits exist," and `budget_vs_actual_this_month`/`life_to_date_spend_by_category`/`monthly_category_totals` were rewired onto it so split amounts land in their real categories instead of all piling into whatever category the transaction happened to keep as primary. Quick Add gained a "Split into categories" toggle with add/remove rows that must sum to the entered amount (checked client-side before enabling submit) — editing splits on an already-saved transaction wasn't supported at first (the inline edit row only edited the single fallback category), a deliberate scope cut flagged at the time and closed in the next entry below.
  - **Receipt attachments**: a private Supabase Storage bucket (`receipts`, one folder per user via RLS matching `storage.foldername(name)[1] = auth.uid()`), a `receipt_path` column on `transactions`, a file input in Quick Add (uploaded server-side inside the same server action, after the transaction row exists so the path can include its id) and in the Transactions edit row (upload or replace an existing receipt), and a 📎 link in the transaction list that fetches a 60-second signed URL on click rather than storing a public link.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean; `get_advisors` shows only the same two pre-existing, unrelated items; a combined rollback-only SQL test exercised all three DB-heavy features together in one transaction (added a $500 liability account, confirmed `account_balance` dropped by exactly $500; added a $100 transaction split 60/40 across two real categories, confirmed `transaction_category_breakdown` and `life_to_date_spend_by_category` attributed to the actual split categories rather than the transaction's fallback category; logged a $50 transfer between two asset accounts, confirmed `account_balance` was unchanged by it) — final computed balance matched the hand-calculated expected value exactly, then rolled back with zero residue confirmed via row counts after.
  - **Closed the split-editing gap.** The Transactions page query now selects `transaction_splits(id, category_id, amount_usd, categories(name))` (was just `amount_usd`/category name, not enough to repopulate an edit form) and also fetches `settings.fx_rate`, passed down as a new `fxRate` prop so the edit row can preview USD conversion the same way Quick Add does. `TransactionEditRow` (`components/tables/TransactionsTable.tsx`) was rewritten to reuse Quick Add's split UI verbatim — a "Split into categories" toggle, add/remove rows, a running total that must match the entered amount before Save enables — pre-populated from the transaction's existing `transaction_splits` when there's more than one. `updateTransaction` (`app/(app)/transactions/actions.ts`) changed from "insert splits if provided" to unconditional delete-then-conditional-reinsert on every save: delete whatever split rows exist for that transaction id, then insert the submitted set only if it has more than one row. This one rule correctly covers all three cases — turning splitting on for a previously single-category transaction, changing an existing split's category/amount composition, and turning splitting back off (falls through to the transaction's own `category_id` via `transaction_category_breakdown`, since zero split rows means "not split"). Verified with a rollback-only SQL test: created a transaction split 60/40 across two categories, simulated the edit-save flow replacing it with a different 25/75 split across two *different* categories, confirmed exactly 2 rows existed afterward (the new set only, no leftover rows from the old set), then confirmed zero residue after rollback. `tsc --noEmit`/`npm run lint`/`npm run build` all clean.
- **9 more features: recurring auto-post, CSV import, multi-currency, audit trail, search, export, onboarding, and two verification-only items.** A follow-up "what more can we do" pass surfaced 9 ideas; all shipped in one batch. By this point the app had 3 real signed-up users (`abelatnafu.g@gmail.com`, plus two organic third-party signups), which changed the verification strategy for the last two items — see below.
  - **Recurring bill auto-posting.** `recurring_bills.last_posted_date date` + a `post_due_recurring_bills()` function (`security definer`, revoked from `anon`/`authenticated` — trigger-function-style RPC lockdown, same pattern as `handle_new_user`) that finds active bills where `billing_day` matches today and `last_posted_date` isn't already this month, inserts a matching transaction (`currency 'USD'`, `notes 'Auto-posted recurring bill'`), and stamps `last_posted_date`. A light dedupe guard (same date/amount/category already logged) additionally protects against double-posting a bill the user already entered by hand before the job ran. Scheduled via `pg_cron` (newly enabled on the project — it was available but not installed): `cron.schedule('post-recurring-bills-daily', '0 6 * * *', ...)`. Recurring Bills' subtitle now says bills auto-post on their billing day.
  - **Multi-currency, replacing the hardcoded USD/ETB pair.** The whole app previously had exactly one secondary currency slot (`settings.fx_rate`, literally labeled "ETB" in the currency pickers). Replaced with a real per-user `currencies` table (`code, name, rate_to_usd`) — USD stays an implicit, non-deletable base at rate 1; any other currency a user adds shows up everywhere a currency picker exists. `set_computed_fields()` now looks up the rate from `currencies` for non-USD codes (raising a clear error if the code isn't one of the user's own) instead of reading a single fixed `settings.fx_rate`; `settings.fx_rate` itself was dropped after backfilling every existing settings row with a customized rate (`fx_rate <> 1`) into a real `ETB` currencies row — this correctly preserved both real users who had set a nonzero rate and left the untouched-default user with no currencies row (since they'd never picked one). Every currency `<select>` in the app (Quick Add, Transactions/Income edit rows, CSV Import) now maps over `[USD, ...user's currencies]` instead of a hardcoded two-option list. Settings → Financial gained a **Currencies** card (`CurrenciesForm` + `addCurrency`/`deleteCurrency`/`updateCurrencyRates` actions) mirroring the existing Accounts card's add/edit/delete pattern.
  - **Audit trail.** A new `audit_log` table (`user_id, table_name, record_id, action, old_data jsonb, new_data jsonb, changed_at`) plus an `AFTER INSERT OR UPDATE OR DELETE` trigger (`log_audit_change()`, security definer, RPC access revoked once the advisor flagged it as anon/authenticated-callable — same fix pattern as every other trigger-only function in this build) on the tables where a change history is actually load-bearing for trust: `transactions`, `income`, `transfers`, `recurring_bills`, `savings_goals`, `net_worth_snapshots`, `accounts`. Deliberately skipped child tables without their own `user_id` (`transaction_splits`, `net_worth_snapshot_balances`) and non-financial tables (`categories`, `settings`, `key_dates`, `semesters`, `currencies`) to keep the log meaningful rather than noisy. New `/audit-log` page (`AuditLogTable`) filters by table/action and renders a plain-English diff per row (field-by-field `old → new` for edits, a one-line summary for creates/deletes) computed client-side from the raw JSON, not stored redundantly.
  - **CSV bank statement import.** New `/import` page (`ImportForm` + `importRows` action): upload a CSV (parsed with a small hand-rolled RFC-4180-ish parser in `lib/csv.ts` — quoted fields, embedded commas/newlines, `""` escapes — rather than pulling in a dependency for this one feature), map Date/Description/Amount columns from the actual header row, pick a currency and default category/necessity for the batch, preview the parsed rows with computed Income/Expense type (sign-based, with a "flip sign" toggle for banks that export the opposite convention), then import. Positive amounts land in `income`, negative in `transactions`; `importRows` re-runs the same same-date/amount/currency duplicate check `addTransaction`/`addIncome` already use so re-uploading a file (or a file with overlapping date ranges) doesn't create doubles.
  - **Global search.** A new `globalSearch()` server action (`app/(app)/search-actions.ts`) runs parallel `ilike` queries across transaction descriptions/notes, income sources/notes, category/bill/goal/account names, and key-date events, capped at 4–6 results per type. `GlobalSearch.tsx` renders it as a debounced command-palette-style overlay — a desktop trigger pill fixed in the top-right and a mobile FAB fixed bottom-left (mirroring Quick Add's bottom-right FAB) both open the same modal, grouped results link straight to the owning page. Mounted once in `AppShell`, outside the desktop-only top-bar wrapper, specifically so its mobile trigger isn't accidentally hidden by that wrapper's `hidden md:flex` — caught this before shipping by reasoning through Tailwind's `display:none`-hides-fixed-descendants behavior, not by trial and error.
  - **Full data export.** `exportAllData()` (`app/(app)/settings/actions.ts`) pulls every one of a user's rows across all 14 tables (settings, categories, accounts, currencies, transactions + splits, income, transfers, recurring bills, savings goals, net worth snapshots + balances, key dates, semesters) into one JSON object; `ExportDataButton` triggers a client-side download (`lib/csv.ts`'s new `downloadJson`) named `command-deck-export-<date>.json`. Lives in the new Account section of Settings, alongside sign-out.
  - **Onboarding flow.** `settings.onboarding_dismissed boolean default false` + a 3-step modal (`OnboardingModal`) shown on Command Deck only when a user has both zero transactions and zero income logged *and* hasn't dismissed it — checked via real `count`-only queries (`{ count: "exact", head: true }`), not just "does the settings flag say no," so a user who logged something and then deleted it doesn't get re-onboarded. Welcome → set up accounts/currencies → log your first entry via Quick Add; Skip or Get Started both call `dismissOnboarding()` so it never reappears.
  - **Leaked-password protection: confirmed not settable from here.** Checked the available Supabase MCP tools (`apply_migration`, `execute_sql`, project/branch management, etc.) — none expose Auth config, because this is a GoTrue (the Auth service) setting, not a Postgres table or RLS policy a migration could touch. `get_advisors` still flags it every run as the one open item; it's a dashboard toggle only (Authentication → Policies → Password → "Leaked password protection" in the Supabase dashboard), unchanged from when this was first flagged post-launch.
  - **Signup tested end-to-end — with real data instead of a manufactured test user.** While investigating this, found the app already has 3 real signed-up accounts (Abel's, plus two independent organic signups that happened between sessions), not just Abel's — evidence the "open signup" work from the multi-tenant conversion is genuinely live and being used by strangers on the internet, not just tested in isolation. Rather than inserting a throwaway 4th user, verified against these 3 real accounts: confirmed `handle_new_user()` had correctly provisioned all three (3 accounts, 10 categories, 1 settings row each) — one signup even shows they'd manually customized their FX rate in Settings, proof the UI itself works end-to-end for a stranger, not just for Abel. Then ran a role-switching RLS isolation check (`set local role authenticated; set local "request.jwt.claims" = '{"sub":"<uid>",...}'` per user, a temp table granted to `authenticated` to collect results since a temp table's own owner-only permissions blocked the switched role at first) confirming each of the 3 real users sees exactly their own accounts/categories/settings/currencies and nothing from the other two — real proof of data isolation, not simulated.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (20 routes now, up from 18 — `/import` and `/audit-log` are new); `get_advisors` shows only the two pre-existing items (`rls_auto_enable`, leaked-password-protection) plus nothing new introduced by this batch (the one new finding it did catch mid-build — `log_audit_change` being anon/authenticated-callable as an RPC — was fixed the same way as every prior trigger-only function, not left as a known issue). A combined rollback-only SQL test in one transaction covered multi-currency (ETB 1,800 at rate 180 → `amount_usd` 10.00, `fx_rate_used` 180 unaffected by a rate change made *after* insert and reverted before the transaction's own edit), the audit trigger (INSERT/UPDATE/DELETE all logged with correct before/after JSON), and recurring-bill auto-posting (one bill due today posted exactly once even after calling the posting function twice) — all in a single `execute_sql` call from `begin` to `rollback`, since testing established mid-session that this tool runs each call as its own connection: splitting `begin`/assertions/`rollback` across multiple calls left an open, uncommitted transaction that the next connection simply discarded (verified harmless by checking for residue afterward), but the safe pattern going forward is always one atomic call.
- **CSV import tuned specifically for Bank of America** (Abel confirmed it's the only bank he'll ever use with this app). The generic column-mapper from the previous entry was made zero-config for BoA's actual export shape rather than staying purely manual: `findHeaderRowIndex()` (`components/ImportForm.tsx`) scans the parsed rows for the first one containing both a "date"-ish and "amount"-ish cell, skipping the "Description / Generated on / Account type / Balance as of" preamble rows BoA's export always leads with instead of assuming row 0 is the header. `guessColumn()` then auto-maps Date/Description/Amount by header text — critically written to also match BoA's *credit card* export shape ("Posted Date"/"Payee"/"Amount", different column names than the checking/savings export's "Date"/"Description"/"Amount") via substring patterns, not just their checking/savings shape. Payment method defaults to a new "Bank of America" entry (prepended to `PAYMENT_METHODS` in `lib/constants.ts`, now the first suggestion everywhere payment method is entered) instead of the old generic hardcoded "Bank Import" string, threaded through as a real `importRows()` parameter. Copy on `/import` rewritten with the actual BoA download steps and a note that their sign convention (deposits positive, purchases negative) already matches this importer's default, so "Flip sign" should normally stay off. Verified against two hand-built CSVs matching BoA's real export shape byte-for-byte (preamble rows, quoted fields, comma thousands separators, MM/DD/YYYY dates) — one checking/savings, one credit card — run through a standalone Node reproduction of the parsing logic: both correctly skipped the preamble, found the right header row, auto-mapped the right columns despite the differing header names between the two exports, and classified every row's income/expense direction correctly. `tsc --noEmit`/`npm run lint`/`npm run build` all clean.
- **Interaction/motion polish pass, triggered by a real bug: "the table on both monthly and weekly recap isn't fully displayed."** Investigated rather than assumed — the tables *did* have `overflow-x-auto` on their `<Glass>` wrapper already, so the bug wasn't a missing class.
  - **Root cause, and it was systemic, not local to those two tables.** `.glass` in `app/globals.css` sets `overflow: hidden` as plain, unlayered CSS. Tailwind v4's `@import "tailwindcss"` puts every utility (including `overflow-x-auto`) inside `@layer utilities`. Per the CSS Cascade Layers spec, **unlayered styles always beat every layered style, regardless of selector specificity or source order** — so `.glass`'s `overflow: hidden` was silently winning over `overflow-x-auto` on every single table in the app, not just Weekly/Monthly Rollup. Confirmed this was real (not a theory) by inspecting the actual Next.js-compiled CSS output: before the fix, `.overflow-x-auto{overflow-x:auto}` sat inside `@layer utilities` while `.glass{overflow:hidden}` sat outside any layer entirely, and a Playwright check against that real compiled CSS showed `getComputedStyle(...).overflowX` resolving to `"hidden"` despite the element having both classes.
  - **Fix:** moved just the `overflow: hidden` declaration out of the plain `.glass` rule into `@layer components { .glass { overflow: hidden; } }` (Tailwind's own documented pattern for "custom component styles that utilities should still be able to override" — `components` sits below `utilities` in Tailwind's default layer order). Left the rest of `.glass` (background/border/radius/shadow) unlayered since nothing overrides those. Re-verified against the freshly recompiled CSS: `.glass{overflow:hidden}` now lands inside the `components` layer, `overflow-x-auto` still in `utilities`, and a Playwright check on the real output now shows `overflowX: "auto"` with `scrollWidth > clientWidth` — actually scrollable, not clipped.
  - **This bug hit every wide table in the app**, not just the two Abel noticed (those just have the widest tables — 820px/1400px `min-width` — making the clipping most visible). Grepped for the pattern and found it on `TransactionsTable`, `IncomeTable`, `NetWorthTable`, `RecurringBillsTable`, `AuditLogTable`, `KeyDatesTable`, `TransfersTable`, `WeeklyRollupTable`, `MonthlyRollupTable`, and `ImportForm`'s preview table — all nine were fixed by the same one-line CSS change, but each was also upgraded past "just scrollable" (see next bullet).
  - **New `HScroll` component** (`components/ui/HScroll.tsx`) replaces bare `overflow-x-auto` on every one of those nine tables: a client component that tracks scroll position (`scroll`/`ResizeObserver`/`resize` listeners) and renders a soft fade-to-background gradient (`.hscroll-fade-left`/`-right`, new CSS) at whichever edge still has hidden content, disappearing once you've scrolled that direction to the end — a real, always-on visual cue that there's more to see, not just a bug fix that relies on the user discovering the swipe by accident (which is what actually caused the original "isn't fully displayed" impression). Verified the fade renders correctly against the real compiled CSS via Playwright (`getBoundingClientRect`, correct gradient background) and that scrolling the container to the end both reveals the previously-clipped last column and correctly hides the fade.
  - **Momentum scrolling**: `-webkit-overflow-scrolling: touch` added globally to `.hscroll` and to every other `overflow-x-auto`/`overflow-y-auto` element in the app (modal/sheet bodies, the Settings sub-nav row) — iOS Safari doesn't do inertial/momentum scrolling by default on overflow containers without this, so swipes on a physical device would have felt stiff/dead even after the clipping bug was fixed.
  - **Entrance animations for every sheet/modal/popover, all of which previously just snapped open/closed instantly** (confirmed zero `@keyframes` existed anywhere in `globals.css` before this). Added four keyframes (`fade-in`, `sheet-up`, `modal-pop`, `popover-pop`) and matching classes (`.anim-backdrop`, `.anim-sheet` — slides up on mobile, cross-fades/scales-in on desktop via a `768px` media query matching the app's existing `md:` breakpoint, `.anim-modal`) applied to: Quick Add's sheet + backdrop, the mobile "More" sheet + backdrop, Global Search's modal + backdrop, the onboarding modal + backdrop, and folded directly into `.cal-pop` (the date-picker popover) as a `popover-pop` animation. These are real `@keyframes` rather than CSS transitions specifically because the elements are freshly *mounted* on open (conditional `{open && ...}` rendering) — a transition needs an existing before-state to animate from, which a just-mounted node doesn't have, whereas a keyframe animation just plays on its own regardless of prior state. Automatically respects both `prefers-reduced-motion` and the existing manual Settings → Appearance → Reduce Motion toggle, since both already force `animation-duration: 0.01ms !important` globally — verified this holds for the *new* animations too via Playwright's `page.emulateMedia({ reducedMotion: 'reduce' })` against the real compiled CSS (all four collapse to ~0s; normal motion plays at their intended 0.16–0.28s durations).
  - **Tactile press feedback**: `.btn`/`.btn-primary` gained `transform: scale(0.97)` on `:active` (previously opacity-only) with `transform` added to their existing transition list; the Quick Add FAB and Global Search's mobile floating trigger gained matching `active:scale-90` press animations; the desktop settings-avatar link gained `active:scale-90 hover:opacity-90`. `.cal-cell` (date-picker day cells) already had `:active { transform: scale(0.9) }` with a proper transition from an earlier phase — confirmed still intact, not regressed by any redesign since.
  - **Missing hover/active transitions filled in** across nav and lists that had color/background states but no `transition-colors`, so they were snapping instead of fading: the mobile bottom tab bar and its "More" button, every link and the sign-out button inside the More sheet, the sidebar's "View insights →" link and desktop sign-out button, and the hover background on every `<tr>` across the six tables that have one (`TransactionsTable`, `IncomeTable`, `NetWorthTable`, `RecurringBillsTable`, `KeyDatesTable`, `TransfersTable`) plus Global Search's result rows.
  - **Confirmed already fine, not touched**: the segmented control (per-button `transition: background` cross-fade — a different but equally smooth mechanism from a sliding-thumb indicator, left as-is rather than rebuilt), Recharts chart animations (no `isAnimationActive={false}` anywhere, so Recharts' default enter animations are active on every chart), `StatCard` (correctly has no hover state since it's a static display, not a control).
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (still 20 routes); every claim above checked against the actual Next.js-compiled CSS output via headless-Chromium/Playwright — computed styles, real scroll behavior, real animation names/durations, both normal and reduced-motion — not just "the code looks right."
- **Apple Wallet redesign (post-launch, supersedes the light "Apple flare" dashboard above for the app's visual identity — the light system's tokens are gone from `:root`).** Abel asked for a from-scratch design pass "like a developer at Apple," sharpened mid-turn to "use the apple wallet theme" specifically — card stacks and material finishes, not another Settings-app HIG pass. Workflow: proposed a design plan, built a real interactive mockup (`command-deck-mono.html`-style single-page concept with 5 iPhone-frame screens) as a Claude Artifact plus 5 PNG screenshots for approval, then — once Abel picked "Implement the Wallet redesign" from an `AskUserQuestion` disambiguating what "deploy everything" meant (nothing new existed in the git repo yet; the mockup had been scratchpad-only) — built the approved concept into the real app. This is the fifth full design swing (Liquid Glass Monochrome → Neo-Luxury → Apple/iOS dark → light Apple-flare dashboard → Wallet), and the first to add real schema, not just re-theme existing screens.
  - **Real schema change, not just a UI reskin**: added a nullable `account_id uuid references accounts(id) on delete set null` to both `transactions` and `income` (`supabase/migrations/20260713090000_add_account_id_to_transactions_income.sql`, plus indexes) — the "tap a card, see that account's real transactions" interaction the Wallet metaphor promises doesn't work if entries can't be linked to an account, and faking it off the existing net-worth-snapshot balances would have been dishonest about what's actually stored. This is additive/non-breaking: existing rows (all of Abel's and both other real users') get `account_id = null` and simply don't show up on any account's detail page until re-entered or edited with an account picked — flagged as an explicit, honest tradeoff rather than backfilled with a guess, since there's no reliable way to infer which historical entry belonged to which account.
  - **`globals.css` collapsed to a single dark palette.** The `:root[data-theme="dark"]` block and the `@media (prefers-color-scheme: dark)` block (from the light "Apple flare" phase) were deleted outright and their values promoted into the base `:root` — Wallet, like real Apple Wallet, ignores the system Light/Dark setting and always renders on a dark canvas. `html`/`:root` `color-scheme` flipped from `light` to `dark`. This is a deliberate, second instance of this app breaking its own "give both themes equal care" default (the first was the Apple/iOS pass's true-black-OLED choice) — here the justification is that Wallet's whole visual identity (card finishes catching light, a true-black canvas) doesn't have a coherent light-mode equivalent, matching how Apple's own Wallet app behaves.
  - **New card-finish design tokens**: five gradient finishes (`--finish-titanium-*`, `--finish-steel-*`, `--finish-gold-*`, `--finish-rosegold-*`, `--finish-graphite-*`) plus `.wallet-card` (shared shape: 16px radius, a `::after` diagonal shine overlay reused from the mockup's `linear-gradient(120deg, rgba(255,255,255,.14), transparent 40%)`) and `.finish-*` classes applying each gradient. Graphite is reserved specifically for liabilities regardless of a card's assigned rotation slot — a debt card is meant to visually read as different stock, not just a different color, mirroring the mockup's credit-card screen.
  - **`components/wallet/AccountCard.tsx` + `CardStack.tsx`** (new directory): `AccountCard` renders one account as either a `"peek"` variant (name only, blank middle since it'll be covered by the card in front, footer showing a masked pseudo-card-number — last 4 chars of the account's real UUID, so it's deterministic and unique rather than a fake placeholder — and the signed balance) or an `"expanded"` variant (name + masked id combined into one label, a large tabular balance in the middle, footer showing "Available balance"/"Outstanding balance" + an Asset/Liability tag) — mirroring exactly how the real Apple Wallet only shows a card's full balance on the front-most card and lets the peeking cards behind communicate through their footer strip alone. `CardStack` takes a flat account list, assigns finishes by rotating `[titanium, steel, gold, rosegold]` by index (graphite is the forced liability override, not part of the rotation), and positions each card absolutely with a 64px peek offset and descending z-index — the same peek math from the mockup (equal 170px card heights, 64px top offsets, NOT `transform: scale()`, which was the mockup's original peek-visibility bug from the design-mockup phase). Empty state (zero accounts) renders a single titanium card inviting the user to add one in Settings, rather than an empty stack.
  - **Command Deck's "Accounts" panel replaced with the real `CardStack`** (`app/(app)/page.tsx`) — was a flat `Glass` list of rows; now a fanned stack of real account cards, each linking to `/accounts/[id]`, with a thin "Net Worth" total strip below it (same total math as before, just restyled to match the mockup's `.nw-strip`). `NetWorthHero` (the dark hero chart card) was explicitly kept, not replaced — it's a genuinely different, valuable piece of functionality (net worth over time with a 1M/6M/1Y/ALL range toggle) that the card-stack concept was never meant to subsume.
  - **New `/accounts/[id]` route** — an account detail page: a back button + account name header, the account's `AccountCard` rendered in `"expanded"` variant as a hero, then its real transactions and income (queried by `account_id`, not the account-total/snapshot math used elsewhere) merged and grouped into Today/Yesterday/dated sections, matching the mockup's expanded-card-plus-transaction-list screen. Empty state explains plainly that only entries tagged to an account going forward will show up here, rather than silently looking broken for Abel's untagged historical data.
  - **Account picker threaded through entry points**: Quick Add gained an "Account (optional)" `<select>` shared by both the Expense and Income forms (Transfer mode already had its own from/to account selects from an earlier phase, untouched); `TransactionsTable`/`IncomeTable`'s inline edit rows gained the same picker. `addTransaction`/`addIncome`/`updateTransaction`/`updateIncome` (`quick-add-actions.ts`, `transactions/actions.ts`, `income/actions.ts`) all read/write `account_id` and additionally `revalidatePath` the specific `/accounts/[id]` route when one is set, so a freshly-tagged entry shows up on its account's detail page without a hard refresh.
  - **Appearance settings simplified for a dark-only app**: removed the Theme (Light/Dark/Auto) and Accent Color cards from `components/settings/AppearanceSettings.tsx` entirely — both are meaningless once the app no longer has a light mode or a swappable accent hue (Wallet's blue is fixed, matching real Wallet's own fixed-blue chrome). Deleted `components/ThemeToggle.tsx` (grepped first to confirm no other references). `app/layout.tsx`'s pre-paint init script dropped its `theme`/`accent` `localStorage` handling, keeping only `glass`/`reduceMotion` (both still meaningful — Liquid Glass intensity and the motion-reduction toggle aren't theme-specific). `SettingsShell`'s Appearance section caption updated from "Theme & display" to "Liquid glass & motion" to match what's actually still there.
  - **Scope call, stated plainly**: this pass did not migrate every remaining screen (Insights, Settings sub-pages, Transactions/Income table chrome) to the card-stack visual language — those already share the same dark token system via `globals.css`/`Glass`/`.material` and re-themed automatically without code changes, which was the point of the token-first strategy carried through every prior redesign; only Command Deck's Accounts panel and the new detail route needed bespoke Wallet-specific markup, since those are the only places the card-stack metaphor itself applies.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (21 routes, up from 20 — `/accounts/[id]` is new); grepped for `data-theme`/`prefers-color-scheme`/`ThemeToggle`/stray `accent`-localStorage references across `app`/`components`/`lib` — zero remaining; screenshotted `/login` (the one page reachable without a live authenticated session) to confirm the dark-only palette applies with no flash; built a standalone harness against the real Next.js-compiled CSS output (not hand-approximated) rendering a 3-card stack (titanium/steel/graphite-liability, correct 64px peek offsets and masked IDs) and two expanded-card variants (gold, rosegold) side by side to visually confirm finishes, signed balances, and the peek/expanded layout split all render as designed.
- **Usability pass — "make this even more easier to use."** Abel confirmed via `AskUserQuestion` that all four proposed directions mattered: faster logging, smarter search/navigation, native mobile feel, and simpler forms. Shipped in one batch, no new schema beyond one nullable-safe lookup pattern (no migration needed — reads existing tables).
  - **Merchant memory in Quick Add.** New `lookupMerchantMemory(kind, text)` server action (`app/(app)/quick-add-actions.ts`) does an exact case-insensitive match on the typed description/source against the most recent matching transaction/income, returning its category/account/payment-method/necessity. `QuickAddFab` debounces (450ms) a lookup as the user types, prefilling only the fields they haven't already touched (necessity needed a separate `necessityTouched` flag rather than falsy-checking, since its untouched default — "Necessary" — is itself a valid manual choice) and surfacing a small "✨ Filled in from your last '…' entry" caption. This required converting category/account/payment-method/necessity/description/source from uncontrolled `defaultValue` fields to controlled state in `QuickAddFab`, a bigger refactor than the feature itself.
  - **"Repeat last entry."** New `getLastEntry(kind)` action clones the most recent transaction/income's fields (not its date — today's date stays the default, since "repeat" means "log the same coffee again today," not "re-date an old entry"). A "↻ Repeat last" link sits next to the Expense/Income/Transfer segmented control; disabled/absent logic wasn't needed since a no-prior-entry response just no-ops.
  - **Smart currency/account defaults.** Quick Add now remembers the last currency actually used (`localStorage` key `quickadd_last_currency`, written on successful submit, restored on mount — guarded against a saved code that's since been deleted from the user's currencies) instead of always defaulting to USD. When a user has exactly one account, it's auto-selected via the `accountId` state's initializer (`accounts.length === 1 ? accounts[0].id : ""`) rather than an effect, so there's no flash of "no account" before a mount-only effect fires.
  - **Progressive disclosure.** The currency `<select>` is dropped entirely (replaced by a hidden `currency=USD` input) when a user has never added a second currency — most users never will, and a permanent USD-only dropdown was dead chrome. Same logic for the account picker: hidden (backed by a hidden input carrying the auto-selected id) when there are 0 or 1 accounts, since there's nothing to actually pick.
  - **⌘K / Ctrl+K launcher.** `GlobalSearch` now has a window-level keydown listener (mounted unconditionally, not just while open) that toggles the modal open/closed — a real global shortcut, not scoped to a focused input. When the query is short/empty, `NAV_LINKS` (already the single source of truth for the sidebar/More-sheet routes) is filtered by label and shown as a "Pages" group above the record-search results, so typing "in" jumps straight to Income/Insights instead of hitting the old "type at least 2 characters" dead end. Desktop trigger pill grew a `⌘K` hint badge.
  - **Pull-to-refresh.** New `components/ui/PullToRefresh.tsx` wraps `AppShell`'s `<main>` (app-wide, not just three pages — cleaner than wiring it per-page and no less correct). Touch-only (`window.matchMedia("(pointer: coarse)")` gate so it never intercepts desktop trackpad/mouse scroll), only arms when `window.scrollY === 0` at touch-start, rubber-bands the pull with a 0.5x damping factor up to 96px, and — the part worth calling out — the actual refresh runs inside `useTransition` (`startTransition(() => router.refresh())`) rather than a fake timed spinner, so `isPending` reflects the real RSC refetch completing, not a guessed duration.
  - **Installable PWA.** `app/manifest.ts` (Next's special-file convention, auto-wires the `<link rel="manifest">` — confirmed in the real rendered `<head>`) plus generated icons via `next/og`'s `ImageResponse`, not external asset files: `app/icon.tsx` (32×32 favicon), `app/apple-icon.tsx` (180×180), and a dynamic `app/icons/[size]/route.tsx` (192/512, both `any` and `maskable` purpose) — all rendering the same blue-square-plus-bank-glyph mark already used for the sidebar wordmark, so the installed icon matches the in-app brand instead of introducing a new one. `public/sw.js` is a deliberately empty-fetch-handler service worker (no caching at all — this app is fully dynamic Supabase data, and a caching SW risks stale reads or auth-cookie interference) registered by a tiny `ServiceWorkerRegister` client component, existing purely so browsers count the app as installable. `InstallPrompt` listens for `beforeinstallprompt` (Chrome/Edge/Android) to show a real "Install" button via the captured event's `.prompt()`, and separately UA-sniffs iOS Safari (no `beforeinstallprompt` support there) to show a one-time "tap Share → Add to Home Screen" tip; both check `display-mode: standalone`/`navigator.standalone` first so an already-installed user never sees it, and both dismiss permanently via `localStorage`.
    - **Caught by testing, not just written correctly:** the new manifest/icon/service-worker routes initially all redirected to `/login` — `proxy.ts`'s matcher only excluded static image *extensions* (`.png`, `.svg`, etc.), and these routes serve images without a file extension in the URL (`/icon`, `/manifest.webmanifest`, `/icons/192`). Fixed by extending the matcher's negative lookahead with exact-match exclusions (`manifest\.webmanifest`, `sw\.js`, `icon$`, `apple-icon$`, `icons/`) — found by literally `curl`-ing each new route against a local dev server and seeing 307s before the fix, 200s with real `image/png` content-types after.
  - **Swipeable mobile rows.** New `components/ui/SwipeRow.tsx` (Mail.app-style swipe-to-reveal-Delete) used by new `TransactionMobileRow`/`IncomeMobileRow` components, which replace — on mobile only (`md:hidden`, with the existing table now `hidden md:block`) — the horizontally-scrolled table with a swipeable card list. Swipe is an enhancement, not the only path: every mobile card keeps plain, always-visible "Edit"/"Delete" text links too, so nothing is lost for accessibility or a mouse-driven mobile-viewport session. Required extracting `TransactionEditRow`/`IncomeEditRow`'s inner `<form>` into standalone `TransactionEditFields`/`IncomeEditFields` components (no `<tr>/<td>` wrapper) so the exact same edit form — including the split-category UI — renders correctly inside both a desktop `<tr>` and a mobile `<div>` card without duplicating ~150 lines of split-handling logic twice.
    - **Caught by lint, not guessed:** the first `SwipeRow` draft read `draggingRef.current` directly during render (for the live drag-transform and the "skip transition while dragging" check) and `eslint-plugin-react-hooks`'s `react-hooks/refs` rule correctly flagged both as "Cannot access ref value during render." Fixed by mirroring the ref into a `dragging` boolean **state** (set alongside the ref in the same touch handlers) and reading that in JSX instead — the same ref+state pairing `PullToRefresh` already used correctly from the start.
  - **Inline guidance for the two genuinely non-obvious flows.** Quick Add's Transfer form gained "Moving money between your own accounts — a transfer never counts as income or spending" (transfers are the one entry type that's easy to mistake for income+expense); the Split-into-categories toggle (both Quick Add and the now-shared `TransactionEditFields`) gained "One purchase, multiple budget categories — e.g. a Target run that's part groceries, part household." Transfers/Recurring Bills/Currencies already had adequate `PageHeader` subtitles and in-card copy from earlier phases — checked each before adding anything, rather than captioning everything indiscriminately.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (still 21 routes — the new `/icon`, `/apple-icon`, `/icons/[size]`, `/manifest.webmanifest` are Next special/generated routes, not user-facing pages, so they don't add to the app's route count the same way); confirmed via `curl` against a local dev server that every new route returns real `image/png`/`application/manifest+json` content after the proxy-matcher fix; screenshotted `/login` post-changes to confirm zero regression from the matcher/layout edits; viewed the generated 512×512 icon directly to confirm the glyph renders cleanly at full size. Merchant memory, repeat-last, pull-to-refresh, the swipe gesture, and the install-prompt banner are all behind live Supabase auth this sandbox can't reach — verified those by careful code review (controlled-field wiring, debounce/cleanup correctness, ref/state discipline) rather than a live click-through, consistent with this project's standing limitation on screenshotting authenticated pages.
- **8 more features, plus a deliberate no-op on removal.** Asked "what more features does it need," then to add 8 more and remove anything unnecessary. Given two explicit `AskUserQuestion` rounds on scope (all 8 features) and removal (Key Dates / Semester Planner) both came back "no preference," proceeded to build all 8 and — since removal is destructive (real rows: 10 Key Dates, 2 Semesters) and "no preference" isn't the same as "yes, delete this" — left both features in place rather than guessing at deletion.
  - **Schema** (`20260715090000_alerts_recurring_income_payoff_goal_link.sql`): `settings.low_balance_threshold` (nullable opt-in), `accounts.interest_rate_pct` (nullable APR, liabilities in practice), `savings_goals.account_id` (nullable link). New `account_balances` view — a live per-account balance (`starting_balance + income − transactions + transfers_in − transfers_out`), deliberately scoped to `kind = 'asset'` only: a transaction tagged to a *liability* account is ambiguous in this schema (a charge vs. a payment both just look like "a transaction on this account"), so rather than compute a plausibly-wrong debt figure, liability balances stay owned by the existing net-worth-snapshot/variance system. `savings_goal_progress` rebuilt (dropped/recreated, not `create or replace`, since the new `account_id` output column isn't at the end) to `coalesce(account_balances.balance, saved_so_far_usd)` — linked goals track a real account live, unlinked goals keep the manual field. New `recurring_income` table + `post_due_recurring_income()` (mirrors `post_due_recurring_bills()` exactly: same due-today-and-not-already-posted-this-month check, same dedupe-by-date/amount/source guard, same `security definer` + revoked RPC access, same `pg_cron` daily schedule) auto-posts paychecks the way bills auto-post charges.
  - **Verified before writing a line of app code**: a single rollback-only transaction created a real asset account, logged income/a transaction/a transfer against it, confirmed `account_balances` computed the exact expected number (100 start + 40 income − 10 expense + 20 transfer-in = 150), confirmed a linked `savings_goal_progress` row picked up that same 150 automatically, and confirmed `post_due_recurring_income()` posted a paycheck exactly once across two calls — then rolled back with zero residue confirmed by row-count check.
  - **Budget & balance alerts**: computed server-side in `app/(app)/layout.tsx` (already the one place that runs on every page) — over-budget categories from `budget_vs_actual_this_month`, low balance against the new opt-in threshold, bills due today/tomorrow from `recurring_bills`. Rendered by a new `AlertsBanner` client component as slim dismissible rows above page content; dismissal is per-alert-id-per-calendar-day via `localStorage`, so a real ongoing problem reappears tomorrow instead of being silenced forever by one dismiss. Settings → Financial gained the threshold input (blank = off).
  - **Recurring income**: new `RecurringIncomeTable` (mirrors `RecurringBillsTable`'s add/edit/delete/active-toggle pattern) added to the Recurring Bills page, which picked up the more accurate name **"Recurring"** since it now covers both directions of automatic cash flow. `NAV_LINKS` label updated to match.
  - **Bulk transaction actions**: `TransactionsTable` (desktop only — mirrors the earlier deliberate scope cut on swipe-gestures being desktop/mobile-split) gained row checkboxes, a select-all header checkbox, and a selection toolbar (recategorize-to-X / delete selected / clear) backed by two new `.in("id", ids)` server actions. Split transactions are a known, accepted no-op under bulk-recategorize (their category comes from `transaction_splits`, not the bulk-updated `category_id` fallback) — not worth special-casing for a convenience feature.
  - **Multi-year trend charts**: Insights gained a "Year over Year" section — a grouped bar chart (one bar series per year, reusing the existing `CATEGORY_SHADES` monochrome ramp already established for Monthly Rollup's stacked bars, spaced `i*3` apart so 2–3 real years read as visually distinct rather than adjacent near-identical grays) plus a by-year income/spend/net summary list. Computed client-side from data already fetched for the page's other stats — no new query.
  - **Debt payoff planner**: a new `PayoffCalculator` client component on liability accounts' `/accounts/[id]` detail page — a real amortization formula when `interest_rate_pct` is set (`months = −ln(1 − rB/P) / ln(1+r)`, monthly `r = APR/1200`), a plain linear estimate (`balance ÷ payment`) when it isn't, and a clear "this payment doesn't even cover the interest" message instead of a nonsense negative/infinite answer when the payment is too low to ever pay it off. Verified the formula against hand-computed cases (1000 @ 20% APR @ $100/mo → 12 months; @ 0% APR → 10 months; @ $15/mo → correctly `null`/never) before wiring it into the UI. Settings → Financial → Accounts gained an APR % input next to each liability's balance.
  - **In-table free-text search**: a Search field added to Transactions' and Income's existing filter bars, matching description/notes/payment-method (transactions) or source/notes (income) client-side against the already-loaded rows — incidentally also fixes a real gap where Income's Source filter was a fixed dropdown of *suggested* sources and could never match a custom-typed one.
  - **QIF/OFX export**: new `lib/financial-export.ts` (`toQif`/`toOfx`, both hand-written — no dependency, since both formats are simple enough not to need one) added as two more buttons alongside each table's existing CSV export, exporting the *currently filtered* rows as a single-account ledger (negative = debit/expense, positive = credit/income) — the same convention real bank exports use. OFX is generated as OFX 1.0.2 SGML (`OFXHEADER:100` + tag soup, not the newer XML variant) since that's the older, more universally-importable shape. Scoped per-table (matching the existing separate transactions.csv/income.csv pattern) rather than merged into one combined register, to avoid a bigger cross-component refactor for a nice-to-have format.
  - **Auto-tracked savings goals**: `SavingsGoalsList`'s add/edit form gained a "Track via Account" picker (asset accounts only, fetched with `kind = 'asset'`); picking one hides the manual "Saved So Far" field entirely (replaced by an explanatory line) since `savings_goal_progress` now derives it live. Linked goal cards show a "🔗 Tracking {account}" link straight to that account's `/accounts/[id]` page.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (still 21 routes — all-new work landed inside existing pages); `get_advisors` shows only the two pre-existing unrelated items; smoke-tested the new views directly against real production data (`account_balances` returned exactly 9 rows for the 3 real users' real asset accounts, `savings_goal_progress` correctly showed `account_id: null` + the old manual `saved_so_far_usd` for both of Abel's real goals since neither has been linked yet) rather than assuming the migration was fine just because it applied without error.
- **"Ethical Horizon" redesign (post-launch, supersedes the Apple Wallet look above).** Abel uploaded a Google Stitch export ("Comprehensive Website Redesign," 4 screens: Strategic Vision, Governance Simulator, Interactive Analysis, Global Impact Map) asking to "implement this UI directly." Inspecting the export first (`DESIGN.md` + `code.html`/`screen.png` per screen) showed it was actually a design concept called **"Ethical Horizon: All-Out"** for a completely unrelated ESG/governance-analytics product ("ABEL ATNAFU" branded nav, a "Global Command Center" for real-time ESG metrics) — not a finance app, and not literally re-buildable as Command Deck pages without inventing fake governance/ESG content that has nothing to do with personal budgeting. Flagged the mismatch and asked via `AskUserQuestion` whether this should be a new project, ignored, or merged in; Abel's reply ("WORK ON THE DAMN DESIG UI/UX" / "make it look like that take UI design") resolved it as **visual-language-only**: reskin Command Deck's existing screens with Ethical Horizon's colors/type/glass treatment, not port over ESG-specific content. This is the sixth full design swing this session (Liquid Glass Monochrome → Neo-Luxury → Apple/iOS dark → Light Apple flare → Apple Wallet → Ethical Horizon), using the same token-first strategy as every prior one.
  - **Palette** (`app/globals.css` `:root`): `--bg: #0b1326` (deep charcoal-navy, DESIGN.md's `surface`/`background`), `--bg-elevated: #131b2e`/`--bg-elevated-2: #171f33`/`--bg-dark: #060e20` (DESIGN.md's `surface-container-low`/`surface-container`/`surface-container-lowest`). Label hierarchy moved off pure-white opacity tiers onto DESIGN.md's `on-surface` ivory-blue (`#dae2fd`) at graduated opacities, separators/fills swapped to the same `rgba(255,255,255,…)`/`rgba(218,226,253,…)` pairing. **`--blue: #10b981` / `--blue-hover: #4edea3`** — "Ethical Emerald" becomes the app's one primary tint (replacing the Wallet-era iOS systemBlue `#0a84ff`/`#409cff`), since that's DESIGN.md's literal `primary`/`surface-tint` value and everything aliased through `--color-tint` re-themes off it automatically. Added `--accent-blue: #3b82f6` (DESIGN.md's `secondary-container`, "Trust Blue") and `--accent-purple: #b090ff` (DESIGN.md's `tertiary-container`) as supplementary accents, plus dedicated `--green`/`--red` tuned to the same doc. `--material-bg`/`--hero-bg`/`--badge-bg`/`--badge-fg` all re-tinted emerald.
  - **Type**: dropped the system-font stack from the Apple/Wallet eras for **`next/font/google` Montserrat** (weights 400–900, DESIGN.md's display/body backbone, "bold heavy weights reserved for data highlights") and **Geist Mono** (DESIGN.md's `label-mono`, "developer-tool precision" for technical data/timestamps) — both wired the same way IBM Plex Mono was originally self-hosted, via `variable` CSS custom properties on `<html className>` in `app/layout.tsx`.
  - **Material philosophy reversed on purpose.** The prior Apple-HIG-derived rule ("material vs. solid split" — blur only on overlay chrome, solid cards everywhere else) is explicitly *not* what Ethical Horizon's own spec calls for: DESIGN.md's "All-Out Glassmorphism" and "Stacked Glass" sections call for blur on every card layer (Layer 1 cards at 20px blur, Layer 2 modals at 40px). `.glass` reverted from the Wallet era's solid `--bg-elevated` surface back to `backdrop-filter: blur(var(--glass-blur, 20px)) saturate(120%)` over a translucent gradient fill, border-radius 18px→16px (DESIGN.md's "ROUND_SIX" 16px base radius); `.glass-dense` similarly regained blur. Page background gained a dot-grid (DESIGN.md's "circuitry pattern... aligned to the 8px grid") plus two layered radial ambient-glow sources (emerald top-left, blue top-right), reviving the "glow behind the page, refracted by the glass" idea from the very first Liquid Glass Monochrome system a few redesigns ago.
  - **Buttons/badges**: `.btn-primary` rebuilt to DESIGN.md's literal component spec — "solid Emerald-to-Blue gradient with a 1px white inner border and a neon outer glow... uppercase Montserrat Bold" (`linear-gradient(120deg, var(--blue) 0%, var(--accent-blue) 100%)`, inset white border, `box-shadow` neon bloom, uppercase bold text — was a flat solid-blue pill with no border/glow under Wallet). `.btn` retinted emerald. New `.neon-glow-emerald`/`.neon-glow-blue` utility classes for the "tight, colored neon shadow" DESIGN.md's Elevation section calls for on hovering/emitting elements.
  - **Charts** (`lib/chart-colors.ts`): `CATEGORY_SHADES` rebuilt as a navy-to-mint 10-step ramp (was cold iOS systemGray) so the one emerald tint still reads as "the" accent instead of competing with a rainbow of category colors; axis/grid/line colors swapped to the new palette (`CHART_LINE_COLOR` emerald, `CHART_LINE_SECONDARY` Trust Blue).
  - **Icons/manifest**: `app/icon.tsx`/`apple-icon.tsx`/`icons/[size]/route.tsx` backgrounds and `app/manifest.ts`'s `background_color`/`theme_color` retinted from the old iOS blue/black to emerald/navy so the installed PWA icon and browser chrome match the new in-app brand.
  - **Sweep for leftover old-token values** (this session's standing verification discipline, same as every prior redesign): grepped `app`/`components`/`lib` for the old iOS/Wallet-era hexes. Found and fixed three real leftovers the initial token/font pass had missed: (1) the sidebar's "Command Center" promo card (`components/nav/AppShell.tsx`) still used the Wallet-era purple radial glow (`rgba(94,92,230,…)` over `#0b0b0c`) and an old iOS sky-blue link color (`#5ac8fa`) — retinted to the same emerald+blue radial pairing already established in `.ledger`, link color switched to `var(--blue-hover)`; (2) the Command Deck net-worth hero chart (`components/charts/DashboardCharts.tsx`'s `NetWorthArea`) still used the Wallet-era purple→indigo→blue gradient (`#bf5af2`/`#5e5ce6`/`#32ade6`/`#7d5cf0`) for its stroke/fill/active-dot — rebuilt as emerald→blue per DESIGN.md's explicit "Holographic Accents: gradients moving from Emerald to Blue" line, and its tooltip/grid/axis colors moved off hardcoded whites onto the new navy/ivory-blue palette; (3) `CashFlowBars`/`SpendingDonut` in the same file had never been updated off a *light-mode* assumption (`background: "#fff"` tooltips) left over from the pre-Wallet "light Apple flare" era, which had been rendering white cards on a dark app for two redesigns running — fixed alongside the hero-chart pass since it was the same file and the same "chart tooltip colors" concern. Deliberately left alone: the Wallet card-finish gradients (`--finish-titanium/steel/gold/rosegold/graphite`, a physical-material metaphor independent of the primary accent), the semantic income/expense green/red (`#34c759`/`#ff3b30`, an explicit, previously-agreed exception to the "no color-coding" rule that predates this redesign and isn't part of it), the Command Deck spending-donut's multicolor category palette (compatible with — not contradicting — DESIGN.md's own "Chips/Tags... color-coded by category" language), and `--indigo: #5e5ce6` in the generic iOS-system-color token block (a supplementary swatch matching Apple's own real systemIndigo dark value, not a primary-accent leftover, and never referenced anywhere in the app).
  - No color-coding for good/bad beyond the one pre-existing green/red exception is still the rule — carried through a sixth design system unchanged, same as every prior redesign this session.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (still 23 routes); grep-swept `app`/`components`/`lib` for the old Wallet/Apple-era accent hexes (`0a84ff`, `409cff`, `5e5ce6` outside the system-color block, `bf5af2`, `7d5cf0`, `32ade6`, the purple `94,92,230` glow, `5ac8fa`) — zero remaining after the three fixes above; screenshotted `/login` and `/signup` at desktop and iPhone widths (dot-grid background, Montserrat display type, translucent glass card, emerald→blue gradient button all render correctly); built a standalone harness against the real Next.js-compiled CSS output (not hand-approximated) rendering a stat card + badge, the ledger hero with its emerald progress bar, both button variants, two wallet card finishes, and a monochrome category-breakdown bar together to confirm the full token set renders consistently, not just that individual files compile.
- **"Senior finance officer" review pass — compliance, close, forecasting, tax, notifications, backup/restore.** Abel asked what a senior finance officer at a huge company would add or remove before rolling this out publicly; resolved the scope via `AskUserQuestion` (full public-product compliance posture, real month-end close + period locking, remove Key Dates/Semester Planner/manual-savings-field/QIF-OFX, and all four headline adds — forecasting, tax reporting, email digest, backup/restore). Eight sub-efforts, each `tsc`/lint/build-clean and, for anything DB-facing, verified with a rollback-only SQL test before being considered done — this pass caught **six real bugs** that code review alone would not have (each fixed and re-verified, not just noted):
  1. **Cascade ordering deadlock in `delete_own_account()`.** `transactions.category_id`/`transaction_splits.category_id`/`recurring_bills.category_id` were left `ON DELETE NO ACTION` (the Postgres default) while every sibling `account_id` FK already correctly used `ON DELETE SET NULL`. Invisible in normal use (no delete-category feature exists), but a full-account purge deletes `categories` and its referencing rows via independent cascade paths from `auth.users` in the same statement, and Postgres doesn't guarantee those resolve in dependency order — it could hit a `categories` row before a `transaction_splits` row still pointing at it. Fixed by switching all three to `SET NULL`, matching the existing `account_id` convention (`category_fks_set_null_not_no_action` migration).
  2. **`audit_log`'s FK to `auth.users` made every account deletion fail.** `log_audit_change()` fires `AFTER DELETE` on `transactions`/`income`/etc. and inserts a fresh `audit_log` row referencing `old.user_id` — but by the time those cascade-deletes run as part of `delete from auth.users`, the parent row is already gone, so the INSERT violated `audit_log_user_id_fkey`. Fixed by dropping that FK entirely (an audit trail legitimately shouldn't be hard-linked to the live identity of the actor it describes — it already stores full `old_data`/`new_data` snapshots for exactly this reason) and having `delete_own_account()` explicitly purge the caller's `audit_log` rows *after* the cascade, since the cascade itself generates fresh "this was deleted" entries that need cleaning up too, not just the pre-existing ones.
  3. **Supabase's `storage.protect_delete()` guard blocks direct SQL deletes on `storage.objects`.** The first `delete_own_account()` draft tried to clean up the `receipts` bucket via a raw `DELETE FROM storage.objects`; testing surfaced the guard ("Use the Storage API instead"). Moved receipt cleanup to the application layer (`deleteMyAccount()` server action calling `supabase.storage.from('receipts').remove()` before the RPC), which goes through the sanctioned Storage API and is scoped by the existing per-user-folder policy.
  4. **`post_due_recurring_bills()`/`post_due_recurring_income()` would have silently broken for every user on a day any one user's month was closed.** Both are single `security definer` calls looping over *every* user's due rows with no per-row exception handling; the new period-lock trigger raises on any insert into a closed month, and an uncaught exception inside a plpgsql loop aborts the whole function call — one user closing their current month would have silently skipped everyone else's auto-posting that day too. Fixed by wrapping each loop iteration in its own `begin/exception` block (`recurring_autopost_tolerate_closed_periods` migration); verified with a two-user rollback test that user A's closed-period bill was skipped while user B's posted normally in the same call.
  5. **`_build_user_backup_json(uuid)` was left callable by anon/authenticated.** It's `security definer` and takes an arbitrary `p_user_id` — without the revoke, anyone could have called its auto-exposed RPC with someone else's uuid and gotten back their entire financial history as JSON. Caught by `get_advisors` immediately after the backup/restore migration (not by inspection), fixed in the very next migration before anything built on top of it shipped.
  6. **Restoring a backup would have silently re-priced every historical foreign-currency transaction.** `set_computed_fields()` (the trigger that locks in `fx_rate_used`/`amount_usd`/`week_number`) reads *live* `settings.tracking_start_date` and `currencies.rate_to_usd` on every INSERT — a naive `restore_from_backup()` that inserted transactions first would have recomputed those figures against whatever the current rate happened to be, not the rate that was actually locked in at the time, silently corrupting restored dollar amounts (the same class of bug already fixed once this session for the transaction-edit path). Fixed by reordering the restore: `settings` and `currencies` are restored to their exact backup-time values *first*, so the trigger naturally recomputes matching figures on its own — no need to fight or bypass it. Verified end-to-end: backed up a $10-equivalent ETB transaction locked at rate 180, changed the live rate to 90 and the tracking start date, restored, and confirmed `fx_rate_used` was still 180 and `amount_usd` still $10.00, not re-priced.
  - **Removed** (Abel confirmed explicitly, not a "no preference" default): Key Dates page and Budget Periods/Semester Planner (route, actions, components, nav entries, `search-actions.ts`/`settings/actions.ts` export references, and the `key_dates`/`semesters`/`semester_pacing` tables — deleting Abel's real 10 key-date and 2 semester rows), the manual "Saved So Far" savings-goal field (goals now require linking a real account; `saved_so_far_usd` stays in the schema untouched for legacy display but the app no longer writes to it), and QIF/OFX export buttons (`lib/financial-export.ts` deleted; CSV export stays).
  - **Compliance.** Real TOTP MFA (`app/(app)/settings/mfa-actions.ts`, Settings → Account card, QR via Supabase's own `totp.qr_code` data-URI SVG — no dependency needed) plus a login-time step-up gate: `lib/supabase/proxy.ts` calls `supabase.auth.mfa.getAuthenticatorAssuranceLevel()` (after the existing `getClaims()` identity check, not instead of it) and redirects to a new `/mfa-challenge` route when a verified factor exists but this session hasn't stepped up yet. Documented plainly in code comments that this is a UX gate, not the data boundary — RLS scopes every table by `user_id` regardless of `aal`, so it doesn't attempt to rewrite every policy to hard-require `aal2`. `/privacy` and `/terms` are real, honestly-written pages (explicitly flagged as not lawyer-reviewed) linked from `SignupForm` and the sidebar footer, added to the proxy's public-route allowlist. Self-serve account deletion (`DeleteAccountForm.tsx`, type-DELETE-to-confirm modal, `deleteMyAccount()` — named to avoid colliding with the pre-existing per-financial-account `deleteAccount(id)` in `AccountsForm`) calls `delete_own_account()`, verified via the rollback tests above. Leaked-password protection and mandatory email verification remain dashboard-only toggles no available tool can flip — flagged for Abel, not silently skipped.
  - **Month-End Close** (`/close`, new `period_closes`/`period_close_accounts` tables): a reconciliation checklist per account (asset accounts get a live `account_balance_as_of(account_id, date)` computed balance to check against a manually-entered statement balance; liabilities are statement-only, matching the existing precedent that this app has never computed live liability balances). Closing requires every account marked reconciled; a closed month is enforced at the database level by `check_period_not_closed()` (a `BEFORE INSERT OR UPDATE OR DELETE` trigger on `transactions`/`income`/`transfers`) blocking edits to dates inside it, not just a UI restriction. Reopening requires a typed reason and is fully audited (`period_closes` reuses the existing generic `log_audit_change()` trigger).
  - **Forecast** (`/forecast`, no new schema): projects balance 3/6/12 months out as `current_balance + (trailing-90-day average of non-recurring net flow × days out) + cycles × recurring income − cycles × recurring bills` — deliberately not "life-to-date average + recurring on top," which would double-count since historical recurring postings are already inside any life-to-date average. Non-recurring is isolated via the real `transactions.is_recurring` column and (for income, which has no such column) `notes = 'Auto-posted recurring income'`. What-if scenario inputs (extra income/expense per month, a one-time expense in a chosen month) recompute client-side only, never touching real data. Chart built per the `dataviz` skill, reusing the app's existing validated `CHART_LINE_COLOR`/`CHART_LINE_SECONDARY` pair rather than introducing new chart colors.
  - **Tax Report** (`/tax-report`, new `transactions.is_tax_deductible` column): a year selector, category breakdown for the year (via the existing split-aware `transaction_category_breakdown` view, so split transactions attribute correctly), and a deductible-transactions list with CSV export for both. The deductible flag lives on the transaction as a whole (not per-split) since deductibility is a property of the purchase, not how its cost is spread across budget categories.
  - **Email notifications** (`settings.notify_weekly_digest`/`notify_budget_alerts`/`notify_bill_reminders`, Settings → Account card): implemented as Vercel Cron-triggered Next.js Route Handlers (`app/api/cron/weekly-digest`, `app/api/cron/daily-alerts` + `vercel.json`), not Supabase Edge Functions — this app has never used Edge Functions and is already fully Vercel-hosted, so a Route Handler matches its existing architecture rather than introducing a second deployment surface. Uses a new service-role Supabase client (`lib/supabase/service.ts`) to read across every opted-in user, which surfaced a real design constraint worth recording: several views this app relies on everywhere else (`account_balance`, `budget_vs_actual_this_month`) are `security_invoker` with *no* explicit `user_id` filter — they rely entirely on RLS to scope themselves, which the service-role client bypasses. Querying them directly from a cron route would have silently summed every user's data into one number instead of computing it per-user. Caught by reading each view's real `pg_get_viewdef` output before writing the cron routes, not by assumption; `lib/notifications/per-user-queries.ts` replicates the same formulas with an explicit `.eq("user_id", …)` instead. Also fixed `proxy.ts`'s matcher, which didn't exclude `/api/` — Vercel Cron calls carry no browser session, so without the fix every cron invocation would have 307'd straight to `/login` instead of ever reaching the route handler. **Honestly scaffolded, not claimed as fully live**: sending requires Abel to add `SUPABASE_SERVICE_ROLE_KEY` and `RESEND_API_KEY` (and optionally `CRON_SECRET`) as Vercel env vars — documented in both `.env.example` and a header comment in each route file. Until then, `sendEmail()` no-ops with a clear log line rather than failing or pretending to send, so the routes' logic is exercisable via their JSON response (`{ok, candidates, sent, skipped}`) before Abel finishes setup.
  - **Backup & Restore** (`/settings` → Backups card, new `data_backups` table): automatic weekly snapshots via `pg_cron` (`create_weekly_backups_for_all_users()`, keeping the 8 most recent automatic snapshots per user — manual ones are never auto-pruned) plus a manual "Back Up Now." Restore (`restore_from_backup()`) is a single atomic `security definer` function, not application-layer delete-then-insert calls, specifically because the correctness stakes (silently corrupting real financial data) warranted real transactional safety — see bug #6 above for the reordering fix that makes it correct. A restore auto-reopens any closed periods first (itself audited) rather than blocking on them, since a restore is a deliberate, explicitly-confirmed full reset, not a casual edit the close-lock is meant to guard against. Type-RESTORE-to-confirm modal mirrors `DeleteAccountForm`'s pattern.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean throughout (27 routes, up from 23 net of the two removed pages); `get_advisors` (security) clean after each migration, down to only the two long-standing pre-existing items (`rls_auto_enable`, leaked-password-protection) plus every new function's grant state matching its documented intent; grep-swept for every removed reference (`key_dates`, `semesters`, `semester_pacing`, `KeyDatesTable`, `SemesterCard`, `AddPeriodCard`, `toQif`/`toOfx`, `financial-export`) — zero remaining; all 13 new migrations mirrored into `supabase/migrations/` with their real applied timestamps for the record, matching this project's standing convention.
- **"Simple enough for a 10-year-old" pass.** Abel asked for a full visual "one of one" redesign first; `AskUserQuestion` on direction was declined (rejected the tool call), so on the narrower follow-up ("simple enough a 10 year old can use it") this was treated as a scoped usability directive rather than another open-ended design swing, and implemented directly per the session's "make the reasonable call and keep going" default rather than asking again.
  - **Nav restructuring** (`components/nav/nav-links.ts`): the flat 16-item sidebar list became 3 labeled groups — **Everyday** (Home, Transactions, Income, Transfers), **Track & Plan** (Weekly/Monthly Summary, Bills & Paychecks, Savings Goals, Net Worth, Forecast, Insights), **More** (Monthly Checkup, Taxes, Add from Bank, History, Settings) — the single biggest lever for reducing how much a first-time user has to scan before finding anything. Every nav item gained a one-line plain-English `blurb` (e.g. Transactions → "Things you bought"), shown as a hover `title` tooltip on desktop and as visible secondary text in the mobile "More" sheet (grouped the same way, since touch has no hover). Routes are untouched — this is relabeling and regrouping only, zero functional risk.
  - **Renamed for plain language, not because the old names were wrong, but because they assumed financial literacy a new/young user doesn't have**: Weekly/Monthly Rollup → Weekly/Monthly Summary, Recurring → Bills & Paychecks, Net Worth Tracker → Net Worth, Month-End Close → Monthly Checkup, Tax Report → Taxes, Import → Add from Bank, Audit Log → History, and the desktop sidebar's redundant "Command Deck" list item (already the wordmark just above it) → Home, matching the mobile tab bar's existing label.
  - **Every `PageHeader` subtitle across all 16 pages rewritten** from technical/adult phrasing ("Full table — filter, inline edit, CSV export.", "Life-to-date totals — all computed, no manual entry.", "Reconcile each account against your real statement, then lock the period so past history can't be silently edited.") to short, concrete, plain-English sentences a novice can act on at a glance. Two Command Deck labels followed the same logic: "Cash Flow" → "Money In & Out", "Net" → "Left Over".
  - **Monthly Checkup (`/close`) got a deeper pass, not just a rename** — "reconcile a closed period" is a genuinely unfamiliar mental model, so the actual UI terminology changed, not just the header: table columns "Computed (as of month end)" / "Statement Balance" / "Reconciled" → "What We Show" / "What Your Bank Says" / "Matches?"; status pill "Reconciling"/"Closed" → "Checking"/"Locked"; buttons "Close Month"/"Reopen Month" → "Lock This Month"/"Unlock This Month"; every related error string in `app/(app)/close/actions.ts` updated to match. The database-level guard a user hits if they try to edit a transaction inside a locked month from another page (`check_period_not_closed()`'s `raise exception` message) was updated too, via a real migration (`20260718091842_friendlier_period_locked_message.sql`) — otherwise the friendlier in-app terminology would have been contradicted by a raw Postgres error surfacing "period is closed" wording the UI no longer uses anywhere.
  - **History (`/audit-log`) simplification**: the page already mapped raw table names to friendly labels (`TABLE_LABELS`) and raw INSERT/UPDATE/DELETE to "Created"/"Edited"/"Deleted" — the remaining jargon was the UI chrome exposing the underlying database concepts, not the values: filter/column label "Table" → "Type", "Action" → "What Happened", "Record" → "Item".
  - **Touch targets and readability** (`app/globals.css`): `.btn` padding `9px 16px`/`15px` font → `12px 20px`/`16px` font; `.input`/`.select` padding `10px 12px` → `13px 14px`. Deliberately did **not** add a blanket `min-height` to `.input`/`.select`, after realizing it would fight the many dense inline-edit table rows across the app (`TransactionEditFields` and similar) that intentionally override to `!py-1.5 !px-2 text-sm` for compact editing — a `min-height` on the base class isn't touched by a padding override, so it would have silently forced every compact table input back to full height and broken those layouts. The base-class-only bump means standalone/primary buttons and inputs (Settings, Quick Add, forms) get bigger touch targets automatically, while intentionally-compact table rows are unaffected either way.
  - **Sandbox verification limitation, same as every prior redesign**: this environment has no live Supabase credentials (`npm run dev` fails immediately in `updateSession()` — confirmed by actually trying it, not assumed), so the authenticated app can't be click-tested here. Verified instead with a standalone HTML harness built from the real Next.js production build's compiled CSS chunk (not hand-approximated), rendering the actual grouped-sidebar markup and `.btn`/`.input` classes together via headless Chromium — confirmed the three nav groups, new labels, and larger button sizing all render correctly as one coherent screen before calling this done.
  - Verified: `tsc --noEmit`/`npm run lint`/`npm run build` all clean (still 27 routes — labels only, no new routes); grep-swept for every old label string (`Weekly Rollup`, `Monthly Rollup`, `"Audit Log"`, `"Tax Report"`, `Month-End Close`) — zero remaining; `get_advisors` clean after the `check_period_not_closed()` migration, same two pre-existing items as before.
- **Real production bug found and fixed while verifying Weekly/Monthly Summary "work in detail," per Abel's direct ask.** Not a code-review guess — traced the actual `weekly_rollup`/`monthly_rollup` view SQL, then confirmed against live data. Both views generated a *fixed* window (`generate_series(1, 52)` weeks / 12 months) anchored only to `settings.tracking_start_date`, with no relationship to where a user's real data or "today" actually falls — any transaction/income outside that fixed window was silently dropped from both the display and the `running_balance` sum, with no error, no empty state, just wrong (zero) numbers. This wasn't a hypothetical: confirmed live that Abel's own `tracking_start_date` (2026-08-15) sits in the future relative to today (2026-07-18), so all 3 of his real transactions carry `week_number = -4` — below the view's hardcoded floor of 1 — meaning **his Weekly Summary and Monthly Summary have been showing $0 the entire time**, invisibly, since these pages shipped.
  - **Fix** (`20260718093235_fix_rollup_views_hardcoded_window.sql`): both views now derive their window from the real data — `least`/`greatest` across the actual min/max `week_number` (or month) present in `transactions` and `income`, widened to always include the current week/month too (so a brand-new period with zero activity yet still renders one row instead of an empty view). This also fixes `running_balance`'s correctness, not just its coverage: the cumulative sum now starts from the *true* earliest week/month of real activity instead of an arbitrary fixed week 1, so it lands on the right total by the final row regardless of how far `tracking_start_date` drifts from actual usage. `monthly_category_totals` (the category-breakdown chart's data source) turned out not to share this bug — it groups directly from real transaction data with no `generate_series` window at all — but the Monthly Summary *page* derives its displayed month list from `monthly_rollup`, so the same fix transitively restored category-breakdown visibility for those months too.
  - **Verified three ways, not just re-read**: (1) against Abel's real production account — before the fix his real week (`2026-07-11`–`17`) was absent from both views; after, it shows `$670` expenses / `$100` income, matching his actual entered data; (2) cross-checked `running_balance`'s last row against the independent ground-truth `account_balance` view (which computes current balance a completely different way, straight from `accounts`/`income`/`transactions` with no windowing at all) — both landed on exactly `-570`, confirming the fix is mathematically correct, not just "shows something now"; (3) a rollback-only synthetic test with a transaction 500 days before `tracking_start_date` and another 550 days after it — both views now correctly sum to the full `$59` (was `$0`), with sane row counts (80 weeks / 19 months spanning the gap, no explosion) and zero errors for the two real users with no transactions yet.
  - `get_advisors` clean after the migration (same pre-existing two items); `tsc --noEmit`/`npm run lint`/`npm run build` all clean — view column shapes are unchanged, so no application code or generated types needed updating, only the view definitions themselves.
